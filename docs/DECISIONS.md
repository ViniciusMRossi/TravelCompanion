# Lightweight Decisions

Record only decisions that materially affect maintainability or visible behavior.

- **D001 — One app module for V1.**
- **D002 — Firebase deferred from bootstrap.** Group sync already has an interface boundary.
- **D003 — Stable API 36 baseline.**
- **D004 — Runtime state never mutates packaged trip content.**
- **D005 — Real font binaries are added later; design typography remains locked.**
- **D006 — Lifecycle pinned to 2.10.0.** 2.11.0 requires compileSdk 37 / AGP 9.1.0 and cannot build on the API 36 baseline. See `docs/technical/DEPENDENCY-BASELINE.md`.
- **D007 — JDK 17 toolchain is auto-provisioned.** Foojay resolver in `settings.gradle.kts` keeps the pinned JDK 17 baseline working on machines that only have Android Studio's JDK 21.
- **D008 — The packaged trip is the sample Day 9 dataset.** `app/src/main/assets/trip/trip.json` is a copy of `trip-package/sample/sample-trip.json`; the minimal starter could not exercise screens 02 and 05. The approved `02 -> 05` flow opens **Baščaršija**, so that attraction, its audioguide and its rain Plan B were transcribed from the approved prototype into the sample package. Latin Bridge stays in the package as a second attraction that does *not* start the walk.
- **D009 — Trip length comes from the trip date window,** not from how many days are packaged, so "Dia 9 de 21" stays true while content for the other days is produced. The sample `endDate` was `2026-10-02`, a 20-day window that contradicted the approved "21 dias"; it is now `2026-10-03`. `validate_trip.py` checks every `dayNumber` against the window so this class of drift cannot return.
- **D010 — No image-loading library.** Packaged photography is decoded straight from assets; a missing binary falls back to the approved striped placeholder.
- **D011 — A walk names its starting attraction explicitly.** `walk.startAttractionId` was added to `trip.schema.json`. Sharing a `cityId` is not an association: every attraction in Sarajevo would otherwise claim the walk departs from it, and screen 05 would offer "Iniciar passeio" from the wrong place.
- **D012 — Duplicate critical items are merged, never de-duplicated.** The same item is routinely declared twice — curated on the day and in full on the transport that owns it. `distinctBy(id)` silently dropped "Ver ponto de embarque"; `CriticalItem.mergedWith` now unions the action links and keeps the first non-null of every field.
- **D013 — "Offline" is earned, not declared.** A document is only badged offline when `availableOffline` is true *and* `AssetResolver` finds the packaged file. Claiming offline access over a missing PDF is the one failure that only surfaces at a bus station.
- **D014 — `validate_trip.py` checks what JSON Schema cannot:** unresolved references, duplicate IDs, `dayNumber` against the trip window, and documents that promise offline access without a packaged file. Findings are warnings while `contentStatus` is prototype/draft and errors once it is `production`.
- **D015 — Playback is application-scoped.** `PlaybackController` lives on `AppContainer` and the player itself lives in a `MediaSessionService`. A screen-scoped player would stop the audioguide the moment the traveller navigates away or the phone rotates, which is exactly when the phone goes into a pocket.
- **D016 — A narrow `AudioEngine` seam sits between the controller and Media3.** It keeps ExoPlayer out of ViewModels and Composables, and it is what makes playback coordination testable without a device. It is deliberately limited to the operations the app performs.
- **D017 — Audio plays from `asset:///` URIs resolved by `AssetResolver`.** Bundled audio is read straight out of the APK and is never streamed; a guide whose file is absent reports `AudioNotPackaged` instead of failing in the decoder.
- **D018 — No notification permission prompt for the audioguide.** *(Corrected: an earlier revision requested POST_NOTIFICATIONS at the first tap.)* A `MediaSessionService` notification is exempt from the Android 13+ runtime permission, so the prompt bought nothing and interrupted the traveller before a single second of audio had played. The manifest declaration stays for the operational notifications of Phase 7, which do need it.
- **D019 — A documented prototype audio placeholder is packaged.** `trip/audio/attractions/bascarsija.prototype.wav` is synthesised by `tools/make_prototype_audio.py` (CC0, no third-party material: a short sine marker at each chapter start, silence in between) so the real playback flow can be exercised on a device. It runs the approved 720 s, so `ag.bascarsija` keeps `durationSeconds: 720` and screen 05 reads "Ouvir audioguia · 12 min" while the stated duration still matches the file on the device. It is labelled in the asset description and in `trip/audio/README.md`; only its chapter titles describe the placeholder rather than real narration.
- **D020 — Screen 05 keeps its approved action; playback surfaces in the compact player.** Screen 05 specifies one action ("Ouvir audioguia · N min") and screen 09 is the shared-player screen, so neither is reinterpreted. The design system's `TcAudioPlayer` compact variant — "the persistent bottom player … stays available while navigating" — lives in the shell above the bottom navigation. The full 52/68/52 transport is implemented in the same component and waits for screens 07/09.
- **D021 — An unplayable guide never disturbs playback.** A guide whose audio is not packaged is not offered at all (availability is known from content), and requesting one is a no-op: audio that is already playing keeps playing and the state keeps describing it. The UI and the engine can no longer disagree.
- **D022 — A finished guide replays from the start.** After `Ended`, both a fresh request and the play toggle seek to 0 and play, instead of resuming at the end where nothing would happen.
- **D023 — "Not playing" never overwrites a terminal state.** In Media3 `isPlaying = false` means paused, buffering, ended, failed, stopped *or* suppressed, and it can arrive before or after the state change that explains it. `Ended` and `Failed` therefore win over a bare pause signal, in either order, and a guide that ended is not given a resume point by the pause that follows it.
- **D024 — A lost connection drops its queue.** If `buildAsync` fails the pending commands are cleared; if the session disconnects, the player listener is removed and the controller is detached. Replaying a stale `prepare`/`play` after a later retry would resume the wrong guide; the next command reconnects from scratch instead.
- **D025 — Seek and chapter selection are deferred to their approved surfaces.** *(Closed: seek landed on screen 07 in D029, and the chapter list landed on screen 04 in D074. Neither capability was ever exposed anywhere else.)* Both are implemented and tested on `PlaybackController`, but the canonical screens put the −15/+15 transport on 07/09 and the chapter list on 04. Exposing them anywhere else would add a screen, which needs design review (`DESIGN-HANDOFF-v2.md` §10). **Approved decision: option (a)** — no solo player is created, and neither ±15 nor chapter selection is added to the compact player. Phase 2 ships play/pause as its reachable behaviour; the capabilities wait for Phases 3/4/6.
- **D026 — Schema 1.1: every local time names the zone it is written in.** `city`, `day` and both `transportEndpoint`s now require an IANA `timeZone`; a timeline item may override its day and otherwise inherits it. Package times are wall-clock, not instants: nothing records which zone that wall clock is in unless the field says so — harmless inside one zone, wrong the first time a leg crosses one. The `$defs/timeZone` pattern only rejects malformed names, so `validate_trip.py` resolves each name against the tz database (`tzdata` is now a declared dependency in `tools/requirements.txt`, since Windows ships none) and fails instead of passing when it cannot — always, not only once the package is `production`, since a wrong zone is a real defect at any content status. `tools/content_preflight.py` shares the same check via `validate_trip.known_timezones()`. An override equal to its own day is redundant but not reported as an error: it is valid per the schema and the package, and flagging it was never asked for. `walk.startAttractionId` (D011) and every other 1.0 field are unchanged.
- **D027 — `trip-package/production/` is versioned only by its `.gitkeep`.** The folder is part of the source → generated → review → production pipeline, so it has to exist in the repository; its contents are real travel documents, so none of them may. A directory-level ignore stops git descending and makes any negation inside unreachable, so the rule is `production/*` plus `!production/.gitkeep`. `source/private/` — the traveller's own bookings and identity documents, which are authoring *inputs* — is closed the same way by its own nested `.gitignore` (`*`, `!.gitignore`), kept local so the rule sits next to what it protects rather than being restated in the root file.
- **D028 — `trip-package/generated/` is versioned only by its `.gitkeep` too.** The review workspace was left open while `production/` was closed, but the difference is a stage in the pipeline, not a difference in what the files hold: `generated/trip.json` carries the booking locators, QR payloads and document references derived from `source/private/`, and `generated/research/` carries the material they came from (CONTENT-PACKAGE-SPEC.md §3, CONTENT-GENERATOR.md §2/§9). Data that D027 keeps out of the repository does not become publishable by sitting one directory earlier. The rule mirrors D027 exactly — `generated/*` plus `!generated/.gitkeep`, never a directory-level ignore — so the folder still exists for `tools/check_repo.py` and the pipeline still has its workspace. Human review therefore happens on the local files, which is where they were going to be reviewed anyway; nothing about the review step changes, only where it is understood to take place.
- **D029 — Screen 07 has its own transport; `TcAudioPlayerVariant.Full` is screen 09's.** The Phase 2 KDoc claimed `Full` served "screens 07 and 09", but the approved prototype and `TELAS-E-FUNCIONALIDADES-APPROVED.md` give them different treatments: screen 07 is an ink field with a white play button at 56 / 76 / 56dp on the bottom bar, screen 09 is a teal card at 52 / 68 / 52dp. `TcWalkTransport` implements 07's; the stale comment is corrected rather than the design bent to fit it. This is where D025 lands seek: `seekBy` was already implemented and unit-tested and now has its approved surface, and neither ±15 nor chapter selection is added anywhere else.
- **D030 — Walk Mode runs without location rather than not at all.** A foreground service of type `location` is refused outright by Android when the app holds no location permission, and the refusal is a `SecurityException` that takes the app down — observed on the device, not predicted. So the service starts only when the permission is actually held, and `WalkModeService` re-checks on its own in case the permission is revoked in between. The walk still runs: screen 07 says "Sem localização · histórias não aparecem sozinhas" and the audio is untouched, because location stories are enrichment. No walk notification is posted in that state, deliberately — the notification exists to satisfy Android's background-location rules, and with no location there is no background work to announce; audio, if any, already carries its own. The state is not only reachable by mistake: "Apenas esta vez" expires after the walk, and Android auto-revokes permissions for apps left unused for months, which is what a trip app is between trips. Screen 06's location row is therefore a way into the system settings page, since a second refusal makes the dialog stop appearing altogether.
- **D031 — Screen 07's story-arrival scaffold lives in `src/debug`, not behind a flag.** The approved prototype carries a dashed "Protótipo · simular chegada num ponto de história" control and notes it is not part of the product. It feeds the packaged story's own coordinates through the same `WalkModeController.onLocation` a GPS fix uses, so what it exercises is the real trigger decision rather than a bypass; without it, checking story triggering means standing in Baščaršija. It was first written in `main` behind `BuildConfig.DEBUG`, and reading the release DEX showed why that is not enough: both `simulateArrival` and the string "simular chegada" were present in the release binary, unreachable but shipped. A coordinate injector in a shipped artefact is a defect regardless of reachability, so the scaffold moved to `src/debug` with a null-returning counterpart in `src/release`, and the label moved with it. Verified again on the rebuilt release DEX: the prototype label is absent from every `classes*.dex`, and what remains of the name is the release source set's own `walkArrivalScaffold`, which returns null. The reproducible check is `grep -ac "Protótipo"` against the release DEX with the debug DEX as a positive control — not the pre-move symbol `simulateArrival`, which no longer exists under that name and so returns zero from any build, proving nothing.
- **D032 — The walk notification is cancelled on stop, not left to the service.** Stopping a foreground service removes the notification it owns, which is why `stopWalk()` originally only called `stopService`. But the walk notification is not always owned by a service: with no location permission none is started (D030), and the walk can still post progress. Ending such a walk left an `ONGOING|NO_CLEAR` notification for a walk that no longer existed, which the traveller cannot swipe away. `stopWalk()` now cancels the notification explicitly as well.
- **D033 — `ACCESS_BACKGROUND_LOCATION` is not declared until something uses it.** It was declared in the Phase 0 manifest and never exercised: Walk Mode holds location through a foreground service while a walk is running (D030), and passive background geofencing is not implemented. A sensitive permission that is declared but unused is friction in Play Store review and has to be justified in a form, while re-adding it in the phase that implements passive discovery costs one line. Removed now, on that basis. *(Re-declared on 2026-09-06 on exactly those terms: passive geofencing is implemented, this is the permission it needs, and nothing else in the app uses it — D103, D104.)*
- **D034 — Firebase is a dependency; `google-services.json` is not.** The SDK is on the classpath unconditionally, but the `com.google.gms.google-services` plugin is applied only when `app/google-services.json` exists — the plugin fails the build when the file is missing, which would make the repository unbuildable for anyone who has not been handed one (D002). Without configuration `FirebaseApp.initializeApp()` returns null, the repository reports `Disabled`, and everything else behaves identically; with the file dropped in, plugin and runtime both light up with no code change. The file stays out of the repository and out of these notes: no project ids, database URLs or keys are recorded anywhere versioned.
- **D035 — Sign-in is anonymous and invisible.** Database rules should be able to require `auth != null` rather than being open to anyone holding the URL, and the brief allows authentication the traveller never sees. Anonymous sign-in gives that: no account, no e-mail, no screen, nothing to enter — which keeps the "no login, no pairing, no code" invariant intact while the group's runtime state stops being world-writable. Failure to sign in makes the group unavailable, never an error local playback hears about.
- **D036 — Screen 09 renders packaged content, and stories still have no image.** The approved handoff for screen 09 specifies a hero photograph of the place, but schema 1.1 gives `story` no asset field and no link to the attraction that has one — `story.latin-bridge` and attraction `latin-bridge` correspond only by naming, which the package never promised. Resolving the hero by guessing `story.x` owns `img.x.hero` was written and then removed rather than shipped. Screen 09 therefore shows the striped placeholder captioned with the story's own name. The modelling fix is an `attractionId` on `story`, which drags a schema version, three package migrations and the guide with it — a content phase, not a screen detail. Recorded as not implemented rather than left to be discovered.
- **D037 — The group may move this phone, but never replace what it is playing.** *(Corrected: the screen that was said to own the decision did not act on it — see D042.)* `syncCorrection` can seek, pause or resume the local player to match the group. It never acts on a `Load` of its own accord: following one blindly would let the other participant swap the guide this traveller is listening to without them asking. The restraint is covered by a test. What the original wording described as already settled — "the screen that offers 'Ouvir juntos' owns that decision" — was only half written: nothing anywhere loaded the group's guide, so a phone with nothing playing could not join a shared listen at all. The owner is now real and is named in D042: arriving on screens 08/09 is the traveller's ask, it is spent once, and it can only ever load a guide onto a player that is holding none. A phone that is playing something is still never swapped, whoever asks.
- **D038 — The group is called reconnecting on this phone's own clock, not when Firebase says so.** *(Corrected: an earlier revision measured silence — see D039.)* Screen 09's amber note is how §3.3 reaches the traveller: the group failed, the audioguide did not. Relying on Firebase's own `.info/connected` to raise it does not work — with the radios off and screen 09 open, the SDK stayed silent for the full two minutes it was watched on the device while the screen went on reading "Sincronizado". So the controller keeps its own clock and its own threshold: twenty seconds without evidence that the group is reachable means reconnecting. Twenty is long enough that a hiccup does not flicker the screen and short enough that two people standing together are told while it still matters. What counts as evidence is D039. The check is a method the ticker calls, so it is driven by a clock in tests rather than by waiting.
- **D039 — Liveness is an acknowledged round trip, not the absence of silence.** D038 first read "nothing arrived for twenty seconds" as the group having gone away. On the device that turned out to be backwards: two people listening to the same guide write nothing for minutes at a time, so a perfectly healthy shared listen looks exactly like a dead one. Screen 09 was watched sitting idle and duly accused a reachable group of having failed after twenty quiet seconds — while edits made from outside the app were still landing on it within a second. Silence cannot be the signal in the other direction either, because Firebase raises events for this phone's own writes before they reach anyone, so an arriving snapshot is not proof that anything is listening. So the phone asks instead: `heartbeat()` refreshes this participant's `seenAt` every five seconds and reports only whether the *server acknowledged* the write, which a Realtime Database write does only once it has actually landed. Four unacknowledged beats — the same twenty seconds — turn the screen amber; one that lands turns it back. The beat doubles as this phone's presence refresh and as the tick that re-reads everyone else's, so liveness costs one small write and no extra traffic, and it only runs while screen 09 is open. Verified on the device in both directions: ninety idle seconds with the radios on left the screen clean, and thirty seconds with them off raised the amber note while the audio played through the outage without a gap. It has one consequence that had to be closed rather than accepted: making the group's snapshot arrive every few seconds puts every branch of `syncCorrection` on the same loop, and nothing publishes a stop when a guide simply ends — so a finished shared listen left the group node reading "playing" and asked each phone to resume a guide that was over, once per beat. A correction whose expected position is at or past the end of the loaded guide is therefore `None`. Found by reasoning about what the heartbeat changed and proved with a failing test, not on the device, which is why it is recorded here rather than among the device findings.
- **D040 — Screen 09 tells one truth about this phone, in both places it appears.** The traveller's own row read "Sincronizado" while the note directly under it read "Não foi possível sincronizar o grupo agora" — observed on the device, and both cannot be true. The original reasoning was that this phone knows its own playback without asking anyone, which holds only while it can still reach the group; when it is this phone that lost the server, it is not in step with anybody. Status and the traveller's own row therefore move together through one function, and nothing else moves with them: the other participants' rows are news only the group's snapshot can carry. Those rows are now real rather than assumed — each phone's `seenAt` is compared against server time, so someone who walked out of signal shows amber rather than being reported as keeping up until Firebase's own `onDisconnect` eventually removes them.
- **D041 — The three seconds of the countdown are not drift.** `startTogether` publishes the position playback is at *now* with an anchor three seconds out, and the audio does not stop while 3–2–1 is on screen. Until the anchor arrives, `expectedPosition` holds the group at its published position — correctly, since that is where playback *will* begin — so the gap between it and the local player widens by a second per second. The tolerance is two seconds, which means most of the countdown window is over it, and any snapshot arriving inside the window seeked the player backwards to the start position, mid-narration. The heartbeat guarantees snapshots inside that window. `syncCorrection` therefore answers `None` for any group whose agreed start moment has not arrived, for seeking and for resuming alike; a start nobody has reached yet moves nobody. Loading is decided before that gate on purpose — brief §5 says both devices preload before the moment arrives. Derived by reading the arithmetic rather than seen on the device, then reproduced by a failing test before it was fixed: three snapshots inside one countdown produced one `seekTo` that should not exist.
- **D042 — Arriving on screens 08/09 is the ask; it is the only thing that can load the group's guide.** The prototype has one way into a shared listen — the "Ouvir juntos" button under screen 07's transport — and it is the same button on both phones; the approved sheet for screen 08 calls it the transition between *asking* and "o áudio começar **nos dois aparelhos**", and brief §5 says both devices preload. So arriving here is this traveller's consent, and the controller decides what it means once the group answers: with nothing shared yet, propose a start (unchanged); with a listen already running on the guide this phone holds, follow it and publish nothing; with nothing playing here, take the group's guide at the group's position; with a *different* guide playing here, keep it. Three of those four were missing. The one that loads is a one-shot invitation spent inside `applyCorrection`, so the group still reaches the player through exactly one function and a phone that is playing something is never swapped (D037). Two consequences worth naming. First, publishing on every arrival was how the last person to open screen 09 dragged everyone else to their own position, as often as they liked — proposing is now only for when there is nothing to join. Second, two phones on different guides both read "Sincronizado", which is the failure with no symptom; screen 09 now says so on the row it is true about and in the note under the list, with the amber dot and the words "Ouvindo outra história" — *(Confirmed: approved. It is a fifth participant state, next to the four the boards draw, in their register and with no network vocabulary in it.)* It is reported only on the phone that is out of step: the group's payload carries one shared guide and no per-participant one, and widening it is a payload change this correction did not make.
- **D043 — The Phase 4 commit's trip prose is reverted; stories are written in the content phase.** `story.latin-bridge` was given a title, three paragraphs of specific historical claims, an asset description and a chapter title inside a commit about synchronization — with no `SOURCES.md`, no `FACT-CHECK.md`, and at least one claim (that Princip "havia desistido do plano naquela manhã") that is interpretation rather than established fact. `content/RESEARCH-AND-FACT-CHECK-RULES.md` exists precisely to stop unsourced travel prose reaching the runtime package. Both `app/src/main/assets/trip/trip.json` and `trip-package/sample/sample-trip.json` are back to what they were before Phase 4 and remain byte-identical to each other; the story is `"Narrativa de exemplo."` again. Reverting rather than retro-fitting sources: the content phase is not now, and a fixture that reads as placeholder cannot be mistaken for authored content.
- **D044 — The countdown belongs to the load, not to the arrival.** Screen 08 is a transition *into* playback, so it must not run when there is nothing to transition into. Setting it on arrival meant a traveller who tapped "Ouvir juntos" while the group was paused watched 3–2–1 count out and land on "Comece um audioguia para ouvir junto" — said about the very listen they were joining, and flatly contradicting what this repository claimed screen 08 was given to the joiner for. Of the two ways out named in review — do not start the count until there is something to enter, or give the wait a state of its own on screen 09 — the first is taken: the second needs copy the approved design does not carry, and one such item is already open. The count is now started by [loadIfInvited] at the moment the guide actually goes onto this player, which is the same instant as before for a group that is playing, and does not happen at all for one that is not. The invitation is kept while waiting, so the join happens when they start again. Two lines in the same method were also in the wrong order: the ask was spent before content was asked whether the guide is even packaged, so a guide missing from this build voided the invitation and no later snapshot could use it. Content is asked first now. Not reachable with the current package — found by reading, covered by a test.
- **D045 — A shared listen that has run past its own guide is over.** The group's `playback` node has no expiry: nothing writes a stop when a guide ends, and nothing writes one when everybody walks away, so it comes back the next day still saying "playing" with yesterday's anchor. The first time two devices ran together this was the first thing they hit — the traveller who tapped "Ouvir juntos" was treated as joining that dead listen rather than starting one, so no 3–2–1 and nothing published; the second phone, holding no guide, joined it and landed at 12:00 of a 12:00 guide, stopped, while the first played on at 02:58. Both screens read "Sincronizado". `isFinished` answers the question from data already in the payload — the anchor, the position, and the guide's own packaged length — and a listen that has run past its guide is treated as no listen at all, for joining, for divergence and for correction alike. D039 already refused to *resurrect* a finished guide on a phone that had it loaded; what was missing is that a phone with nothing loaded has no duration to compare against, so the same dead node could still be loaded onto it. One consequence had to be closed in the same breath: an ask that resolves to "nothing to join and nothing to propose" is now kept rather than spent, because the first answer the second phone gets is often exactly that, and spending it left the traveller watching the other phone count down without them. **Not fixed:** a *paused* node from an earlier session is still followed, and will pause a phone that arrives the next day. Telling it from a group that is genuinely paused needs either a session concept in the payload — a change, not a correction — or a timeout that would break a legitimately long pause. Recorded, not guessed at.
- **D046 — Corrections happen on the beat, not on the snapshot.** `GroupSyncState` deliberately maps `seenAt` away — the screens show a state, not a timestamp — so two consecutive snapshots of a group that is simply listening are *equal*, and the repository's `StateFlow` drops them. `onGroupState` therefore stopped being called as soon as two people settled, and every correction stopped with it. On the Galaxy S24 and the emulator: one traveller paused on screen 09 and was never resumed, the two drifted a full minute apart, and both screens went on reading "Sincronizado" with no note — the failure with no symptom, again, and one no single-phone pass could produce, because the earlier passes made the group's state change by hand in the Firebase console, which is exactly the case that still worked. Brief §5 says each device computes the expected position *periodically*; it now does, from the beat that D039 already runs, against the last snapshot the controller holds. Leaving and re-entering screen 09 was the only thing that used to fix it, which is what pointed at the cause. `applyCorrection` remains the single point of contact with the player.
- **D047 — Screen 09's transport acts on the shared listen.** Nothing published what the transport did: `startTogether` was the only writer and it always wrote `isPlaying = true`, so `SyncCorrection.Pause` and `SyncCorrection.Resume` — written, unit-tested, and recorded in D037 as how the group moves this phone — could only ever be produced by editing the database by hand. That is how "the group pausing this phone" came to be recorded as verified on hardware in Phase 4: the other side was the Firebase console, not a phone. With two devices the truth showed: one traveller paused, the other narrated on, they drifted a minute apart, and both screens said "Sincronizado". The approved design gives both phones the same full transport on screen 09 with no host and no client, and the product forbids inventing one, so pause, resume and ±15 there are things the group does. The payload is unchanged — the same five fields, anchored at now rather than three seconds out, because this corrects a listen that is already running rather than agreeing a new one. A phone that is not in the shared listen still publishes nothing (D042). *(Confirmed: "either traveller may pause the group" is the intended reading. The approved design gives both phones the same full transport on screen 09, with no host and no client, and the product forbids inventing one; there is nobody for the transport to belong to except the group. The question is closed, not carried.)*
- **D048 — "você" on screen 06 is the participant holding the phone.** The pill was labelled by list position — `index == 0` — with a comment above it correctly describing what it should have done. With one phone it read correctly by accident, because the traveller was the first participant in the package. On Érika's phone screen 06 said "Vinícius · você". The local participant id already reaches the route; it now reaches the state and the label compares ids, like screen 09's list already did.
- **D049 — Inside a shared listen, the system transport is a local control and the group wins.** Screen 09's transport publishes what it did (D047); the notification, the lock screen and a headset's own button do not. So during a shared listen a pause from any of those is undone by the next beat — the group is still playing, and D046 makes the correction actually happen — and the audio comes back within about five seconds. This was watched on the emulator, deliberately, as the proof that D046 works; what was not written down is what it means for the traveller, and that is the point of this entry. It is recorded as a decision rather than left as a side effect: while listening together, the shared listen is the thing being controlled, and the one surface that speaks for the group is the one the approved design draws for it. The consequence is that Phase 2's verified "headset media buttons pause and play both ways" holds *outside* a shared listen and not inside one, which both phase notes now say. The alternative — a pause from the system transport publishing too, exactly as screen 09's does — was flagged and is now *discarded, not pending*. A button pressed through a coat pocket is an accident, and an accident must not stop two people's narration; screen 09 is where one speaks for the group, and the approved design draws exactly one such surface. The system transport keeps controlling this phone alone, and the group puts it back.
- **D050 — The transport's state is what was asked for, not what the engine has confirmed.** `seekTo` always wrote the new position into `PlaybackState` itself and then told the engine; `play` and `pause` told the engine and waited for its listener. That left every reader depending on when the answer comes back, and D047 added a reader that acts in the very next statement: screen 09 publishes `isPlaying` to the group. It worked on both devices — a remote pause followed in 312 ms — because Media3's `MediaController` happens to report from inside the call, and nothing in this repository fixes that. Had it ever differed, pausing on screen 09 would have published `isPlaying = true` and asked the whole group to resume, with no error anywhere. `play` and `pause` now record the intent before asking the engine, the same shape `seekTo` has, which removes the ordering question for every reader rather than for this one. Two rules are kept: the intent never overwrites `Ended` or `Failed`, which is what `onPlayingChanged` already refuses to do, and a callback arriving inside the engine call lands afterwards and wins, because what the engine reports is truer than what was asked for. Covered by a test that turns the fake engine's synchronous reporting off — the thing that had been making the old ordering pass by construction — and by one that goes through `togglePlayPause()` as production does. Found by reading, not on the device.
- **D051 — Two things the first visual pass found, and what each was.** Four field passes had gone by without anyone comparing a screen with the approved prototype; the first that did found two real divergences and fixed both. *Screen 05's hero was not drawing at all.* `TcHero` painted its photograph and its striped placeholder with `fillMaxSize`, which resolves to nothing when the incoming maximum height is infinite — and screen 05's hero takes its height from its own content (`heightIn(min = 200.dp)`) inside a scrolling column, which is exactly that case. The card's own surface showed through and the white eyebrow, title and subtitle sat on near-white: sampled at `#FFFDF8` where the prototype specifies the cool stripes `#8FA3A0`/`#7F948F`. Screens 01 and 09 were unaffected only because their heroes have a fixed height. `matchParentSize` covers whatever the hero turns out to be without being what decides it. *Screen 02's location line could be crushed to one character per line.* The header gives the location column the remaining width and the offline chip its own; at 360dp with the system font at 1.5 what remained was a few dp, and a `Text` given a few dp does not shrink — it wraps per character, and the screen became a vertical alphabet twenty lines tall. The approved header draws that line, and the day counter under it, on one line each, so that is what they now do, ellipsizing rather than breaking. What was left of it — that at that width and that font the location elided to "…", losing the city — is answered by the rule in D055, which decides what gives way when a line will not fit.
- **D052 — The system bars are light, because the app is.** `enableEdgeToEdge()` with no arguments picks the status- and navigation-bar icon appearance from the *system's* night setting. This app implements one palette — `lightColorScheme`, paper everywhere, with ink used as a surface inside screens rather than as the window background — so with night mode on, white icons landed on the paper strip above every screen and the clock became unreadable. `themes.xml` already declared `windowLightStatusBar`; edge-to-edge simply was not told. It is told now. The token file does carry a dark palette, and nothing implements it: that is a phase of its own, not a bug, and until it exists the bars must not pretend otherwise.
- **D053 — Screen 07's header names what is in the traveller's ear, or names the walk.** Arriving at a story whose audio this build does not carry put "TOCANDO AGORA · Latin Bridge" above a transport still running Baščaršija — watched on the device. The screen is read at a glance with the phone coming out of a pocket; a false claim about what is playing is the one thing it must not make. The header now names the arrived story only while the player is on that story's guide, and otherwise names the walk — which is what this screen already does before the first arrival, so no new words were needed. This is D021's ground: whether a guide is packaged is knowable from content before anyone taps, so the screen can simply not say the thing rather than correcting itself afterwards. The walking instruction still follows the arrival rather than the audio: where to walk is true whether or not the story could be narrated. `WalkStopState` carries the story's `audioGuideId` so the comparison is against the player and not against content, and four unit tests hold it.
- **D054 — One sentence for waiting on the group.** Two moments had no words of their own and it was the same gap: arriving with the group paused, and the second or so — longer on a cold start, where the anonymous sign-in is a round trip — while the group is being asked. Both showed "Comece um audioguia para ouvir junto", which is true of this phone and false about the situation, and which tells someone who has just tapped "Ouvir juntos" to go and do something instead. The sentence is **"Esperando o grupo. Assim que alguém começar, você entra junto."** It says what is being waited for and what will happen, in the shape of the approved amber note — a statement, then the reassurance — with no network, no protocol and no waiting time in it, because synchronization describes itself by its result. It replaces the older sentence outright rather than joining it: since D045 keeps the ask alive until there is a listen to join, a phone holding no guide on screen 09 is *always* waiting on the group, including when the group has nothing yet. There is no remaining moment for "Comece um audioguia" to be the right thing to say.
- **D055 — When a line will not fit, the operational identity wins and the qualifier gives way.** The visual pass found the same question on two screens, and the next screen would have asked it again, so it is answered once here rather than twice by accident. On screen 02's header the city is where the traveller is standing and the country is context, so the city keeps its line and the country ellipsizes; the offline chip beside them is a reassurance, so it shares the row rather than taking it, and gives way before the city does. On screen 09's listener list the name has no substitute and the state word does — the coloured dot next to it says the same thing without words — so the name keeps its line and the label ellipsizes, with the dot measured first so it always survives. Both were real: at 360 dp the location wrapped to three lines and vanished into "…" at a 1.5 system font, and "Vinícius · você" broke across two lines to make room for "Sincronizando novamente". The rule is deliberately about roles rather than about these two screens: *name the thing, then qualify it, and let the qualifier be what shortens.* Verified at 360 × 760 dp and 480 × 1040 dp, at the normal font and at 1.5.
- **D056 — Two guards under the way this app looks, and still no screenshot suite.** *(Extended: both proposals below were approved and are now built.)* The most valuable defect of the previous round — `TcHero` painting nothing, so screen 05's title sat invisible on the card's own surface — was invisible to all 160 unit tests. It was a layout bug: a modifier that resolves to zero under an infinite constraint, which no state assertion can see. The net that would have caught it wholesale is a screenshot suite, and that is still **not** built: it means golden images to maintain and a device or a rendering backend to produce them, which is the "speculative framework" `CLAUDE.md` names. What is built is the two cheap guards, each aimed at this defect rather than at screens in general.

  **The measurement.** `TcHeroGeometryTest` composes the hero in the shape that broke — a height that comes from its own content, inside a vertical scroll, where the incoming maximum height is infinite — and asserts that the backdrop covers it. Both layers carry `TcHeroBackdropTag` so the question can be asked at all. It runs as a plain unit test on the JVM under Robolectric, in `testDebugUnitTest` with everything else, so it needs no device and no separate command.

  *(Corrected, twice, and both corrections are the point.)* The first version composed `TcHero`, which in this build can only ever reach the placeholder branch — no photograph ships yet — so swapping `matchParentSize` for `fillMaxSize` on the `Image` left it green. That branch is not dead code: it becomes the live one the day real photography lands, which Phase 1 still lists as open. `TcHero` now delegates to an `internal TcHeroWith` that takes the photograph already resolved, and a second case measures it. The first version's *assertion* was also too weak twice over: "at least as tall as the declared minimum" let a broken `Image` through, because it does not measure to nothing but to its painter's aspect ratio — a square painter in a full-width hero came to 320dp and cleared a 200dp floor. Equality between backdrop and hero passed too, because a backdrop measured with `fillMaxSize` still *sizes* its parent, so the hero inflated with it and the two were equal and both wrong. The assertion is now both halves: the hero is the height its own content asked for, and the backdrop is exactly the hero. With the defect on the `Image` it fails with *"the hero must be the height its own content asked for expected:<200.0.dp> but was:<320.0.dp>"*; on the placeholder, with the backdrop at zero.

  **The rule.** `tools/check_repo.py` reads every composable whose name mentions a hero and fails if a layer inside it is sized with `fillMaxSize`. This catches the class before it is written: the token file already names `TcCityHero` and `TcAttractionHero` as components still to come, and each will have the same backdrop-under-content shape. It runs in the check that already runs first in every verification, costs nothing, and needs no Kotlin toolchain. It is also the guard that had the wider reach of the two: it covered the `Image` branch from the start, which is how the reviewer's first attempt at reintroducing the defect was caught while the test slept through it. It has been wrong twice as well — its first version took the first `{` after the function name and found a default argument's `= {}` instead of the body, and it matched only names *ending* in "Hero", so extracting `TcHeroWith` silently took the layering out of its reach. It now matches any name that mentions a hero. A guard that cannot fail is worse than no guard, so every version of both of these was made to fail before it was kept.

  The cost is one test dependency (Robolectric) on the unit-test classpath and `unitTests.isIncludeAndroidResources`. No new module, no instrumented source set, no golden images, no second command. The visual pass remains the real net for everything these two do not cover, and that is still a person looking at screens and writing down what they saw.
- **D057 — Room arrives with voice memories, because that is when it is needed.** Phase 0 has carried "add Room when structured runtime persistence is first needed" since the first commit, and every phase since has managed without it: participant identity, playback positions and walk triggers are each a single value, which is what DataStore is for, and everything else the screens show comes out of the packaged trip. A memory is the first runtime fact that is a *table* — a list of past recordings, newest first, each with a duration and an author, queried rather than read whole. One entity, one DAO, one database, and a `MemoryRepository` interface two methods wide so Room stops at the data layer and nothing above it knows the difference. No new module. The cost is measured rather than assumed: the full `testDebugUnitTest assembleDebug assembleRelease lintDebug --rerun-tasks` went from 40s to 1m12s, three extra tasks, all of it KSP running the Room compiler. Memories are runtime state and never content: `SCHEMA-GUIDE.md` lists voice memories among the things that do not belong in `trip.json`, nothing in this phase writes to the package, and nothing goes to the group.
- **D058 — Screen 12: what recording does to everything around it.** Three decisions the approved sheet does not make, made here. *The audioguide gives way and comes back.* Nothing in the prototype, the gallery or the sheet says what a playing guide should do when recording starts, so it was asked rather than guessed: it pauses, and finishing or cancelling resumes it — but only if this is what paused it, so a guide the traveller had already stopped stays stopped. On a speaker the narration would otherwise be recorded into the memory; in headphones the traveller would be talking over a voice in their ear. The pause is local and is never published: screen 09 is the only surface that speaks for the group (D047, D049), and the group session is not even joined while screen 12 is open. *The way in is the way that already existed.* The canonical flow is 11 → 12 and screen 11 remains out of scope, so screen 12 keeps the "Gravar memória" shortcut screen 02 has had since Phase 1 rather than bringing a twentieth screen into being. *Where you are comes from what the app already knows.* The city comes from the packaged trip and the place from the walk this phone is running — the same `domain/walk` state screen 07 reads, not a second path to location. When there is no walk, or location was never granted, the memory is saved anyway with the city and the hour: voice is the primary format and the place is enrichment. The promise on the idle screen is that the place and the hour arrive by themselves, and the hour always does.
- **D059 — A clock read during composition is not a clock.** Screen 12's context line took the hour from `System.currentTimeMillis()` evaluated while composing, so it only advanced when something else caused a recomposition — and screen 12 is a screen the traveller stands still on before touching anything, which is exactly when nothing else does. The scope was narrow and worth stating: the timestamp a memory is *saved* with never came from there, it comes from the controller's `epochNow` at the moment recording starts, so the stored metadata was always right and only the line on screen could be stale. Cosmetic, then — except that this screen's whole promise is "O local e a hora entram sozinhos", and an hour that quietly stops arriving by itself undercuts the one sentence the idle state makes. It now ticks in a `LaunchedEffect` at ten seconds, which is more often than a minute-resolution clock needs and rare enough not to wake the screen for nothing. Found by review, not on the device.
- **D060 — The wallet closes the loop D013 opened, and does not lie in the header.** Since Phase 1 every run of `validate_trip` on the runtime package has reported the same two documents promising `availableOffline` with no file in the build. That is not noise to clean up; it is the state of the package and the case screens 13 and 14 exist to handle. D013 already decided the principle on screen 05 — a promise of offline without the file is not badged as offline — and it applies twice here. The header's "Tudo offline" pill is a claim about the *whole* wallet, so it is absent, not softened, the moment one document does not resolve; and the row says so itself rather than leaving the missing pill to be noticed. Opening such a document is a first-class state in D021's shape: the file is not there, the trip data around it is, and that data — times, platform, locator — is usually the part that is actually needed at a counter. So screen 14 draws its ficha from the package rather than from the file, which is what keeps it readable either way. Grouping (Hoje / Transporte da viagem / Seguro e documentos) is a pure function over content and a date, testable with a fixed day, and "today" comes from `TripContent.dayFor` — the current-day logic Phase 1 already has, including how it behaves before the trip starts. There is no second one. The row's booking status comes from whatever the trip says owns the document, the transport or the stay, not from a constant on the screen; it reuses the approved S5 vocabulary (`Reservado`, `Pago`, `Verificar`), and the sheet's "Emitido" was not invented into the schema's enum.
- **D061 — A QR is generated only from real ticket data, and the generator is gated rather than trusted.** Brief §18 says a QR must come from the actual document or actual encoded ticket data, and never from a placeholder. The package's one `generated-from-text` document holds `MOCK-ABC123`, in a trip marked `isMockContent`, and a scannable square holding a fake locator is worse at a counter than no square at all: it looks like it works. So `com.google.zxing:core` is added — encoding only, the pure-Java artefact, no camera, no view, no permission — and the encoder runs only when the package is not carrying mock content. With the trip as it ships, screen 14 says the code arrives with the real ticket and shows the locator, which the driver takes anyway; the approved redundancy line is exactly for this. `embedded` and `generated-from-text` are kept as different values on purpose: collapsing them into one string encoded the asset's *path* into a QR, found while planning the verification rather than in the field. What can be proved without a camera is proved — the matrix is encoded and decoded back, with a locator, a longer ticket payload and accented text. What cannot: **no camera has read a code from this app**, because with this package there is no code to read. That verification is blocked by content, not by a device, and it comes back the day real ticket data does.
- **D062 — QR mode restores what it borrowed, by any exit.** §18 asks for full brightness, a screen that stays awake and minimal UI — and then for the previous behaviour to be restored on closing, which is the half that gets forgotten and the half that leaves a phone at 100% brightness in someone's pocket. Both are window attributes taken and given back by one `DisposableEffect`, so the "Fechar" button and the system back button cannot diverge; QR mode is a state of screen 14 rather than a destination of its own for the same reason, since two ways out would be two chances to forget. A test proves it, and proves it by failing first: with the restoration removed, brightness stays at `1.0` where it should be back to `-1.0`.
- **D063 — The mock-content gate covers a code however it arrives.** D061 gated `generated-from-text` and left `embedded` open, which was the wrong half to guard: §18 says never use a *visual* placeholder QR, and a packaged image is exactly the visual case — if anything more convincing at a counter than a square this app drew, because it was shipped as a picture. A mock package now yields `QrState.MockContent` before the mode is even looked at. Not reachable with the current package, where both documents use `generated-from-text`; closed anyway, because the reachable half was closed for a reason that applies to both. It withholds a code only where one would have been produced: a document declaring `mode: none` has no code in any package, and telling its holder that one arrives with the real ticket would be a different untruth — which the existing test for that case caught the moment the gate was put one line too early. Found by review.
- **D064 — A packaged telephone number is not a button until the number is real.** The package ships `+000000000` for the insurer and the consulate — each carrying a note saying to replace it — and `+387000000000` for the host. D061 refused to encode mock ticket data into a scannable code because a placeholder that looks real is worse at a counter than nothing at all; a telephone is the same argument with a higher price, because it fails at the moment it is pressed, which is the moment it was kept for. So a trip-specific number does not become a dialable row while `metadata.isMockContent` is true. The contact keeps its row — knowing an insurance line exists matters even when this build cannot dial it — and says "O número chega com os dados reais da viagem.", new copy in the register D061 established and asked for rather than invented. Public emergency services are not this kind of data: 112, and the police and ambulance numbers beside it, are facts about a country rather than about this trip, and the schema already separates them into their own fields, so they dial whatever state the package is in. Every call goes out as `ACTION_DIAL`, which needs no permission and leaves the traveller to press the call button; `ACTION_CALL` would need `CALL_PHONE`, and this repository has just finished removing a permission it declared and never used.
- **D065 — A timeline row opens what it refers to.** Screens 15 and 16 had no way in: the canonical flow reaches them from screens the app does not have yet, and the wallet's rows open documents rather than the transport or the stay that owns them. The day's timeline already carries `kind` and `refId` for exactly these, so a `transport` row opens screen 15 and an `accommodation` row opens screen 16. Only rows that point at a screen become tappable — nothing on the timeline gains an affordance that leads nowhere. The current package lists no accommodation on the day, so screen 16 is not reachable from the running app today; that is a content gap the schema already allows a package to close, not a design one.
- **D066 — `fillMaxHeight` is D051 again, in a second place.** The critical card's oxblood rule is a child that fills the row's height, and a row inside a scrolling column has no height to fill until its own content decides one — so it resolved to nothing and the rule did not draw at all. Exactly the shape of the hero defect: a modifier that quietly becomes zero under an unbounded constraint, invisible to every state assertion, found by looking at the screen. `IntrinsicSize.Min` is what screen 02's timeline already uses for the same shape. Worth noting what did *not* catch it: `check_repo`'s rule reads composables whose name mentions a hero and looks for `fillMaxSize`, and this is neither. The rule is deliberately narrow and this is the cost of that; widening it to every `fillMax*` in every composable would flag the many places where it is correct.
- **D067 — Screen 17 is verified without ever completing a call.** The one screen whose whole point is a number that works is also the one screen that must not be exercised the obvious way: no test, on any device or emulator, presses call on 112 or on anything beside it. So the verification is split. `EmergencyDialTest` composes the real screen under Robolectric, presses the real button and asserts the number handed to the launcher is `112`, then `122` and `124`, and that a withheld row hands over nothing — the assertion that would have caught a wrong constant, made without a radio. On the emulator the button was pressed once and the dialer came up with `112` filled in and the green call button untouched, which is where that verification stops. `ACTION_DIAL` is what makes the split safe: it cannot place a call, so the last step is always a person's decision, and no permission is declared for one (D064).
- **D068 — Emergency is reached from "Mais" until screen 19 exists.** Screen 17's canonical entry is the list on screen 19, which is not in this block. Rather than build a finished screen with no way in — the failure D065 had just corrected for 15 and 16 — the "Mais" placeholder carries an "Emergência" row, and the placeholder gained a second action slot to keep "Trocar participante" as well. It is provisional and says so here: when 19 is drawn the row moves into it and the placeholder goes. The alternative, a permanent shortcut somewhere on screen 02, would have put an emergency affordance into the approved Today layout, which the design does not draw.
- **D069 — Three things the sheets ask for that the package cannot yet say.** All three were left out rather than invented, and `trip.schema.json` was not touched — a schema change drags a version and three migrations. *Walking distance at both ends of the leg* (screen 15): the transport legs carry station names and platform, and there is no field for how far the walk is at either end. *The insurance policy number beside the insurer* (screen 17): `emergencyContact` has `label`, `phone` and `note`, and that `note` is an authoring instruction — "Substituir pelo contato real" — not a line to show a traveller, so the row carries the label alone. *A deadline on each Plan B step* (screen 18): here the schema is not the gap, `planBStep.deadline` exists and the packaged plan simply declares none, so all three steps draw without one and `currentStep` treats the first as current because nothing has expired. The first two need a schema conversation; the third needs a package.
- **D070 — The emergency profile is chosen by where the traveller is.** Screen 17 took `emergencyProfiles.first()`, three lines above a city resolved carefully through the same current-day rule screens 02 and 13 use. The schema models this as a lookup by country — a profile requires `countryCode`, a city requires `countryCode`, and `TripContent.emergencyProfile(code)` was already there — so the first-in-list reading was not a shortcut but a different question. On a trip that crosses a border it prints "Kotor · Bósnia e Herzegovina", the class of contradiction D040 named, and the cost is not cosmetic: the police and ambulance buttons would dial another country's services on the one screen that exists for the moment that matters. The profile now comes from the current city's country, and **the country name on the location line comes from the city too** — one fact from one place, so the line cannot pair one country's city with another country's name whatever the profile turns out to be. The fallback, when a package enters a country it carries no profile for, is the first profile, and it is acceptable for a narrow reason: it is a symptom of an incomplete package rather than a normal path, a screen with no emergency number at all is worse than one offering the trip's other country, and 112 — the row that matters most and the one that is always drawn — is the same number across the region this trip covers. The location line keeps telling the truth about where the traveller is while the fallback is in effect, so nothing on screen claims to be local when it is not. No new copy was invented to announce the fallback, because the approved sheet draws none. *The test named for the invariant did not hold it:* `police and ambulance carry the numbers of this country` passed on a fixture with one profile, where "this country's" and "the first" are the same list entry — the `WalletTest` mechanic of the round before, with the name pointing straight at the missing property. Both files now carry a second country, and all four of the new assertions were made to fail against the old implementation before being kept.
- **D071 — "Mais" shows the group it already knows, and does not join to ask.** The Grupo section of screen 19 needs a state per traveller, and the only live source is `GroupSessionController`, whose `join()` D039 deliberately tied to screen 09's `DisposableEffect` because being in the group costs a write every five seconds. "Mais" is a screen people sit on, so joining there would quietly turn a settings list into a heartbeat. It reads the `StateFlow` the controller already holds and joins nothing: a traveller who has opened 09 in this session sees the state from then, and one who has not sees both people as **Offline** — S2's fourth state, drawn with a dot *and* a word like every other. The sentence beneath is what actually carries the meaning, and the approved sheet says as much: "o grupo se reencontra sozinho quando houver internet, sem que nada pare de funcionar" is a promise about §3.3, not a request for live data. The middle option — one read on opening, no heartbeat — was weighed and refused: it still makes a settings screen touch the network, for a fact whose whole point is that it does not matter. No infrastructure word reaches the section, and a test asserts that.
- **D072 — The critical card's buttons wrap, and reuse is what found it.** Screen 02's critical card puts *Mostrar passagem* and *Ver ponto de embarque* in a plain `Row`. Screen 15's own card had already been given a `FlowRow` for exactly these two labels at 360dp with the system font at 1.5, and screen 02's was left alone on purpose — it was verified on hardware and refactoring a proven screen for tidiness is not a reason. Screen 03 reuses that card as the sheet asks ("os mesmos dois botões de Hoje"), and at that size the second button measured **6px wide by 518px tall**: one character per line, the same defect, in the screen it was first found in. So the wrap moves into the shared card and screens 02 and 03 both get it. The lesson is not about `FlowRow`: leaving a known defect unfixed in one place because that place happened to pass is a bet that nothing will ever reuse it.
- **D073 — Screen 03's sheet points at the wrong screen numbers, and the names are what is followed.** The 03 sheet says *Ver transporte (12)*, *Ver hospedagem (13)*, *Documentos deste dia (leva a 10)* and *Plano B deste dia (leva a 17)*. None of those match the canonical v2 index the same document uses in its own headings, where 12 is Gravar memória, 13 is Carteira, 15 is Transporte, 16 is Hospedagem and 18 is Plano B. The buttons are routed by name — transport to 15, stay to 16, documents to 13's document screen, Plan B to 18 — because the names are unambiguous and agree with the rest of the sheet, and each destination makes sense for the button that reaches it. Recorded rather than silently corrected: the numbers in that paragraph look like a leftover from an earlier index, and the sheet is the approved artefact, so someone who owns it should decide whether to fix the text.
- **D074 — Screen 04 is where the chapter list finally lands, and it loads the guide it belongs to.** `seekToChapter` has been implemented and unit-tested since Phase 2 with no approved surface to drive it, and D025 refused to invent one. This is that surface. Two decisions the sheet does not make. *A chapter picked on a guide that is not playing loads that guide and starts there*, rather than seeking whatever happens to be loaded — the list belongs to the city guide, and someone who taps "Período otomano" while an attraction's guide is paused means the city's chapter three, not a jump inside the other recording. *The list is collapsed until asked for*: it is the longest block on the densest screen, and a reader who wants chapter four is deliberate about it, while everyone else is reading the city. The story cards' *Ler* opens the text in place for the same reason it always does here — screen 10 is the canonical reading surface and is not in this block, and expanding beats inventing a twentieth screen. Everything else on 04 is context and not operation: the only hour on the screen is where an attraction sits in the itinerary, which is orientation, and a test asserts no departure, platform or locator reaches the editorial opening.
- **D075 — Hero text needs the cool placeholder, not the paper one.** Screen 04's hero drew the paper stripes (#D7CFBC / #CDC4AE) with the country and the dates over them, and on the emulator both lines all but disappeared — light grey-blue on light beige. Screen 05 had already solved this by asking for `cool = true` and putting white at 85% over the darker green-grey stripes; the city hero and the attraction thumbnails now do the same. Worth stating as a rule rather than a fix: **the paper placeholder is for a hero with nothing written on it, and any hero that carries text takes the cool one.** The day photography arrives this stops mattering, which is exactly why it would have gone unnoticed until it did not.
- **D076 — A Compose test whose window is 320 × 470 px is not testing the screen.** Robolectric's default window is that small, and the first version of screen 04's chapter test "passed" a click that never happened: the button sat below the fold, the injected tap landed outside the window, and it was **dropped in silence** rather than failing — the assertion after it then failed for the wrong reason and would have been easy to explain away. These tests now declare `qualifiers = "w411dp-h891dp-xhdpi"`, and anything still below the fold is brought in with `performScrollTo` before being pressed. A second lesson from the same hour, and the more expensive one: a guard written for D055 on the attraction pills could not tell the two layouts apart — it passed with `FlowRow` and passed with a plain `Row` — so it was **deleted rather than kept**. A test that cannot fail is worse than no test, because it is read as coverage. The wrap stayed (it is the treatment screens 06 and 15 already use, and the packaged pills visibly wrap on the emulator); what went is the assertion that was pretending to prove it.
- **D077 — Two more screens were reading the first element of an array, and one of them wrote it down.** D070 fixed the emergency profile; the same shape was three lines below it and in screen 12. *Screen 17's "onde estão as malas"* took `accommodations.first()`, which is tonight's hotel only on the first night — from the second night of a twenty-one-day trip it is the wrong number, on the emergency screen. It now comes from the day's `accommodationIds`, the mechanism screen 03 was already using. *Screen 12's voice memory* took `cities.first()` for the city it attributes a recording to, three lines from a `placeName` derived carefully from the walk's own state — and unlike the other two this one does not stay on screen: it goes into the `Memory` and into Room, so a recording made in Mostar on day 15 would have been kept as Sarajevo forever. That is the one thing in this app that cannot be redone, which is the argument for putting recording in the application rather than the screen in the first place, and the screen's own promise is "O local e a hora entram sozinhos". It costs `buildAttribution` a `date` parameter; the mechanism is `content.dayFor(date)`, like everywhere else. **Why none of this was caught: the packaged trip ships one city, one stay and one day**, which is precisely the shape in which "the first element" and "the element for today" are the same answer, so every test written against it passed either implementation. `TwoCityTrip` exists now — the package with a second city, a second night and a second day — and anything that resolves something *for a day* is tested against it. Both new assertions were run against the old code first and both failed with the neighbouring city's name.
- **D078 - Screen 10 rises only for the story that was offered, and the walk above it is screen 07 itself.** `decideStoryTrigger` answers `PlayInWalk` or `Notify`, and the approved sheet's three actions - *Ouvir agora*, *Ler*, *Depois* - describe the second. The first was asked about rather than guessed, and the answer is that no page rises for it: a story that auto-played is already in the traveller's ears, which is the whole promise of Walk Mode - headphones, screen off, phone in a pocket - and lighting a screen to announce something already happening works against it. Screen 07 keeps showing the story it is playing, so nothing goes unanswered. *The sheet is drawn over screen 07 rather than as a route of its own*, because the composition the sheet describes - the walk still visible at the top, dimmed, with the story rising from the bottom on a light ground - is literally that: the walk underneath is the real screen 07, scrimmed. The first version drew its own "1 de 2 · ≈ 22 min restantes" strip at the top of the overlay, and on the emulator it printed on top of the identical line screen 07 was already showing; both were illegible. The state carries no copy of the walk's progress now. *The operational line is a measurement, not an estimate*: the distance is the real distance from the fix that triggered the story to the story's own point, and when there is no fix the line simply omits it. **What is not reachable with this package:** the three packaged stories declare `autoPlayInWalk: true` and the walk leaves `automaticStoriesDefault` at its schema default of `true`, so a real arrival always plays and the offered case never happens - the same class of content gap as D065. *(Corrected on 2026-09-07 by D141, and the correction matters more than the new wording: from the first real package until that day this sentence was **false**. No packaged story declared `autoPlayInWalk` at all, and the schema default for that field is **`false`**, so `decideStoryTrigger` answered `Notify` at every real arrival and the case that never happened was the auto-play one - the exact opposite of what is written here. This is the note-that-describes-a-state-which-stopped-existing defect, this time inside DECISIONS itself. D141 adds the field to the three stories and gives the sentence back the behaviour it describes.)* A second debug-only scaffold turns automatic stories off and then arrives, through the same `onLocation` seam D031 already uses, which is how screen 10 was seen on both emulators. `main` still names neither action nor label.
- **D079 - Screen 11 counts what was already tracked, and gives the traveller back the day.** Four summary cards, and no new bookkeeping behind any of them. *Duration* is the walk's own clock - the controller stamps the first start and the completion, so resuming from Paused does not restart it. *Distance* is the route's declared length: nothing in this app measures how far anybody walked, and an odometer would be a second subsystem to keep true, so what is shown is what the package promised rather than a number invented to look measured. *Stories heard* is `playedStoryIds`, which is only ever set when audio actually started - §20 keeps `playedAt` apart from `triggeredAt` for exactly this, and with the packaged walk it reads "0 de 2" because one of its stories has no guide and the other's file is not in the build (D021). *Who listened* is this traveller plus whatever the app already holds about the group: opening screen 11 never asks, because asking would join the group and D039/D071 keep that to screen 09. "Ainda hoje" is Today's own timeline, with the critical item drawn above in oxblood and **excluded from the rows beneath it**, so the same commitment is never listed twice. And screen 11 is what finally makes the canonical **11 -> 12 -> 02** path exist: recording a memory from here pops back to Today, and screen 02's shortcut into 12 stays exactly where it was, because it is a legitimate entry and already tested.
- **D080 - The visible half of the same defect, and a rule so there is no fifth one.** D077 fixed the city a voice memory is *saved* with; three lines above it, the line at the top of screen 12 was still reading `cities.first()`. That one is worse than being wrong on its own: `attributionLine` falls back to it until a recording starts and then switches to the attribution, so the same screen named one city before the button was pressed and another one seconds later - D040's class, on the screen whose whole promise is "O local e a hora entram sozinhos". Both now come from the same answer, resolved from the clock the builder already holds. **And the guard the reviewer approved is built.** Inside `domain/**`, `feature/**State.kt` and `feature/**UseCase.kt`, reading a packaged collection's first element with no predicate fails `check_repo` unless the line - or the `//` block attached above it - says `// fallback:` and why. That is the whole point: a deliberate first-element read is fine, and it should say so where the next reader is rather than in a decisions file they may never open. Three such reads exist today and all three now carry their reason; the rule was made to fail first, against the very line that prompted it, and it names it: `MemoryState.kt:63: val city = content.trip.cities.firstOrNull()?.name`. It is deliberately narrow, like the hero rule: it does not look at `data/`, where resolving a collection by identity is the job, and it does not try to tell a good first element from a bad one - it only insists that somebody said which.
- **D081 - A memory gets its own small player, not the app's.** The handoff draws screen 12 in isolation and thinks in memory-versus-memory; the question it does not ask is memory-versus-audioguide. An audioguide lives on the application, in a media session, with a notification and lock-screen controls, because it exists to survive a pocket and a dark screen - that is what D015 and D017 bought. A voice memory is a forty-second recording listened to while looking at the list, and the handoff draws its progress bar *inside the row*, indented to line up with the title. So it gets a `MemoryAudioPlayer` owned by `MemoryController`, which already holds the file and already lives on the application. Three things follow. *Playing a memory takes the audioguide's place and gives it back only if it was the one that paused it* - not a new mechanism, but the `pausedTheGuide` flag that has guarded recording since Phase 5, with one more caller; the test that holds it for recording now has a twin for playback. *A memory never reaches the media session, the media notification or the lock screen*, where a personal recording would be announced to anyone walking past the phone - confirmed on the S24, where `dumpsys media_session` shows no session for this app while a memory plays and `dumpsys audio` shows the `MediaPlayer` started. *Only one memory plays at a time*, which the handoff asks for and which comes free from there being one player. `AudioGuideRequest` is untouched, so its "always a local `asset:///` URI" invariant still holds, and no screen that depends on the global player changed a line.
- **D082 - The first thing that leaves the phone, and how narrow the door is.** Everything until now went inward: packaged content, a local recording, five fields of group state. Sharing is the first way out, so the `FileProvider` exposes `files/memories/` and nothing else - never `filesDir` - and the manifest entry is declared because it is used, on the same terms as the permissions this repository has removed when they were not. The read permission is granted per-Intent and expires with it. The sheet is Android's: this app draws no destination list, ships no brand logo and has no opinion about where a memory goes; `ACTION_SEND` with an audio MIME and the system does the rest. Sharing never moves, renames or removes the local file, and deleting a memory later removes only this phone's copy - what was sent belongs to whoever received it, and the interface does not pretend otherwise. Two small things earn their place. The provider substitutes a display name, because a memory's file is named by its id per §22 and the share sheet would otherwise offer the traveller "ec5f6817-63b2-...m4a"; on the S24 it reads "Sarajevo.m4a". And the chooser reports the component the traveller picked, which is how the row can say "Enviada para Bluetooth" - best-effort by nature, since a chooser dismissed without a choice reports nothing and the row simply stays as it was. **The handoff's open question is answered by the system**: this app registers no direct-share target and no sharing shortcut, and the S24's own sheet still put "Erika - WhatsApp" first, out of the traveller's own history. Nothing had to be built for it, and the tile stays out of this delivery as the handoff's own fallback says.
- **D083 - A memory stops when its screen goes, and that is the opposite of the guide on purpose.** D015 and D017 put audioguide playback on the application precisely so that leaving a screen does not stop it: a guide survives navigation, a pocket and a dark screen, and there is a notification, a lock screen and a headset button to pause it from. A memory has controls in exactly one place - its own row, on screen 12. Leaving it playing would be audio with nothing anywhere to stop it, which is worse than the interruption. So `stopMemory()` runs when the route leaves, and it gives the audioguide back on the way out. Recording, deleting and opening either sheet stop it too, for the same reason each: the microphone would record it, the file is about to go, and the traveller is about to be looking at something else. The asymmetry is stated here rather than left to be discovered, because it contradicts a decision this repository still holds.
- **D084 - The one row another application reads, made total and testable.** `MemoryFileProvider` is the only surface of this app that other applications talk to and the only place in it that assembles a cursor by hand, and of 306 tests none touched it. Two of its branches were wrong and neither could be reached. *Returning the source cursor from inside `use`*: `use` is inline, so that `return` is non-local - the `finally` closes the cursor before the value leaves the function, and the caller's first call on it throws "attempt to re-open an already-closed object". *Copying an unknown column with `getString`*: it happened to be right only because AndroidX's `FileProvider` emits exactly `DISPLAY_NAME` and `SIZE`. Both were safe only because of an internal detail of a superclass that nothing in this repository pins. The empty case now builds an empty *open* cursor, and the copy is driven by the type the cursor itself declares, so it stays correct whatever columns arrive - no branch that exists on the strength of a guess. The substitution moved into `renameIn`, a function that can be driven directly: what sits above it is AndroidX's and needs no test of ours, and what sits below it is a path strategy that resolves by canonical path, which under Robolectric on Windows disagrees with its own temporary data directory - a property of the test host, not of the provider. The test was made to fail first, against the branch that prompted it.
- **D085 - The third and fourth places a pair of buttons did not fit, found by walking.** D066 fixed the critical card's buttons, D072 moved that wrap into the shared card. The second full visual pass found the same shape twice more at 360dp with the system font at 1.5, both in screens the first pass had covered when they had fewer states: *screen 02's now card*, where "Abrir no Maps" came out 77dp wide and 85dp tall - one word per line - beside a "Ver atracao" that kept its line; and *screen 05's hero*, where the placeholder caption and the city line printed on top of each other, both unreadable, because that hero takes its height from its own content above a 200dp floor and at 1.5 the content grew up into the caption's band. The first is the established answer, a `FlowRow`. The second reserves the caption's band in the hero's content padding, so the hero grows instead of overlapping - and it costs nothing the day a real photograph arrives and the caption goes. Worth naming the pattern rather than the two fixes: **every layout defect this project has found lives at 360dp with the font at 1.5, and none of them is visible to any test.** That is now four rounds in a row.
- **D086 - A screenshot from the Galaxy S24 cannot be compared to a hex token.** The visual method has been "sample the pixel, do not trust the eye" since the first pass, and it worked because the emulator's framebuffer is the app's own colours: sampling screen 19's emergency row there gives `#7A2E2E` exactly, and paper gives `#F5F1E8` exactly. The same row on the S24 samples `#713331`. The phone is on Samsung's automatic screen mode, which transforms the frame before it reaches `screencap`, so a device screenshot is the panel's rendering and not the app's palette. Nothing is wrong with the app, and nothing about this is fixable in it. What follows is a rule for every visual pass from here: **colour is sampled on the emulator, and the device is for what only hardware can answer** - headphones actually connected, audio actually leaving for them with the screen off, the system share sheet with real destinations, the dialer, and whether a screen is legible on OLED. This pass used exactly that split.
- **D087 - The third door, and the one the app actually walks through.** D027 closed `trip-package/production/` and D028 closed `trip-package/generated/`, both on the same reasoning: the traveller's own booking locators, tickets and identity documents are inputs to authoring and never repository content. `app/src/main/assets/trip-production/` was declared ignored in the very first commit and **nothing ever read it** - a grep over `app/src/main` finds the string only in `.gitignore`. So the one directory the runtime loads from, `assets/trip/`, was tracked, and real content had nowhere else to go: copying it there would have put twenty-seven personal documents and a `trip.json` full of real locators into the history permanently, one `git add -A` away, with the two upstream doors standing closed for exactly that. The app now resolves its root from **the presence of `assets/trip-production/trip.json`** - no build flag, no variant, no dependency, on the same terms as `google-services.json` deciding whether Firebase is configured (D034) - and falls back to `trip/`, which stays committed, stays required by `check_repo`, and stays the sample every test reads. The part worth naming is not the lookup but its shape: **the root is one value, not two constants.** `AssetTripRepository.TRIP_ASSET_PATH` and `AssetResolver.TRIP_ASSET_ROOT` were separate strings that happened to agree, and a build that read the real `trip.json` while resolving its PDFs under `trip/` would show twenty-seven documents and open none of them - indistinguishable, from outside, from a build with no documents packaged at all. `TripAssetRoot` is resolved once and carried, and the test that holds it records which path was opened and asserts the assets resolved under the same directory. Both branches were made to fail first: pinning the resolver to `Sample` fails the agreement test, and pinning `resolve()` to `Sample` - which is what this repository did until today - fails the production one.
- **D088 - A note that is about 112, not about emergency numbers.** Screen 17's 88dp button is labelled from `profile.generalEmergency.phone`, and the sentence under it was a hard-coded string: "Funciona sem credito e sem chip local." That was true of every profile the app had ever loaded, because every one of them was 112. The real package has a seventh, `countryCode: BR`, whose general number is **190** - reached on days 1 and 20, in Sao Paulo - and the claim is not true of it: 190 is not the number you can dial from a phone with no SIM anywhere in Europe. The approved sheet is the authority and it is explicit about the pairing: *"Botao unico de 88dp 'Ligar 112' com a observacao de que funciona sem credito"* - the note is written about that number. Suppressing it for others is what the sheet says, not new copy, so nothing went to the design confirmation stack. The decision sits in `buildEmergencyState` rather than in the composable, where it is a unit test rather than a screenshot, and it is the same place D070 put the choice of which profile to show at all.
- **D089 - A guard that stopped guarding the moment a second day existed.** `TodayUseCase.activeIndexOf` has carried the right intent since Phase 1 - *"Days other than the real current date have no 'now' - reading tomorrow must not claim something is happening"* - and it compared `day.date` against the `date` it was asked for. On screen 02 that argument is the real current date and the guard works. Screen 03 reuses the same use case with a **browsed** date, so the comparison became a day against itself, always agreed, and the pill was placed on whatever item the wall clock fell on. Standing on 5 September and paging to the 15th said the 07:15 flight to Corfu was under way. Invisible until now for a content reason and not a code one: the sample package has **one** day, both arrows are null, and there is nowhere to browse to - the twenty-day package is what made the branch reachable. The fix is to pass what the function actually needs, the date the traveller is living, separately from the date being rendered; the test drives a browsed day and a lived one and was made to fail first, `expected no pill`. The general lesson is the one this repository keeps relearning: **a parameter that is usually equal to another parameter is not a guard, it is a coincidence.**
- **D090 - The third time the first element of a collection was the wrong answer, and the first time it changed country.** D077 fixed the city a voice memory is saved with, D080 the city screen 12 names. Screen 17 still resolved *where the traveller is* as `day.cityIds.firstOrNull()`, and on a day that crosses a border that is the city they **left that morning**. Opened on day 3 of the real package it read "Amsterda - Paises Baixos" and offered the Dutch consulate to someone sleeping in Ksamil, directly above a row saying "Guesthouse em Ksamil - Onde estao as malas" - Albania's ambulance (127) and the Tirana duty phone were not on the screen at all. This is D070's defect surviving in the half D070 did not touch: D070 fixed *which profile for a country*, and left *which country* reading the first city. The answer is the one screens 02 and 03 already use, `baseCityId` first, because the day declares where the night is spent and that is what "where the traveller is" means on a travel day. Worth noting what made it visible: the fixture in `EmergencyStateTest` had days touching one city each, so first and base were the same value and the two readings were indistinguishable - the same shape that let D070 through, one field over. The new test crosses a border inside a single day.
- **D091 - The file is in the phone, so it opens.** Twenty-seven documents went into the APK this session and screen 14 could not open one: `DocumentAccess.Packaged.assetPath` was resolved, `DocumentState.isPackaged` was computed, and **no screen read either** - the approved sheet draws a ficha and a QR mode and stops there. The Wallet said "Tudo offline" over files the traveller had no way to see. Asked rather than invented, and built on the answer. The shape is D082's, in the other direction: this app renders no PDF - a viewer is a specialist app and stays external like the maps and the dialer - so the one document asked for is copied out of the assets into `cache/documents/` and handed over with `ACTION_VIEW` and a read grant that expires with the Intent. **Cache, and one file at a time**: the original is in the APK and can be written again, so the copy is disposable, and the directory is emptied before each write because a stale copy of a changed document is a wrong ticket at a counter. The provider's paths grew by exactly one line, `cache-path documents/`, never the cache itself. The control appears only when the file is in the build, which is the rule the QR control already follows, and it is secondary to it: at a counter the code is what is scanned and the file is what is asked for when there is none. Verified on the S24 - the LATAM ticket opened in Samsung's reader at six pages.
- **D092 - The deadline decides in `domain/`, and `AlarmManager` is only told.** The brief's last unbuilt V1 item, "critical notifications are scheduled locally", is one hard part and a lot of plumbing, and they are separated. The hard part is **which items ring and at what instant**, and it is the first thing in this app that *computes* with `day.timeZone` rather than printing it: until now the zone decided what a screen writes, and here it decides when a phone wakes. So `criticalAlerts` is a pure function over `TripContent` plus a cutoff - no Context, no framework - and it is where four rules live that the package can break. *No `actionByTime`, no alarm*: an item can be worth reading on screen 02 without being a deadline. *No instruction, no notification*: the body is `criticalItem.instruction` and nothing is composed, because a sentence written for someone under time pressure is content and the content has it - **silence beats invented text**. *One alert per item, at its earliest occurrence*: an accommodation's critical item is pulled into every day of the stay, so Kotor's "check-in fecha as 21:00" reads on three days and is one deadline, on the night of arrival - the real package proves it, twelve alerts out of fourteen readings. *Nothing in the past*. The instant is `day.date` + `actionByTime` read in the timeline item's zone when it declares one and the day's when it does not, and `CriticalAlert` carries the `ZoneId` beside the `Instant` on purpose: every zone in this trip except Athens and Sao Paulo is +02:00 in September, so two zones that disagree still produce the same instant, and an instant alone cannot show the right rule was applied. The test asserts the zone. Routing reuses `Routes` - made internal rather than copied - because two places that must agree about "stay/{stayId}" is the defect, not the coupling.
- **D093 - An exact alarm, asked for once, and honest when refused.** A deadline that arrives late is the defect the warning exists to prevent, so these alarms have to be exact - and since Android 12 that costs a permission. `USE_EXACT_ALARM` needs no asking and Play policy reserves it for alarm clocks and calendars, which this is not. `setAlarmClock()` is exact without any permission but shows the next one as a system alarm, and twelve "alarms" the traveller never set across twenty days is the system lying on this app's behalf. So `SCHEDULE_EXACT_ALARM`, declared - and **actually obtained**, which is the part that matters after 05daab4 removed a permission declared and never used. The S24 proved why: the first build read the permission, found it denied (the Android 13+ default), and registered every alarm with a **one-hour window**. The app now opens Android's own settings screen for it - the system's screen, like the dialer and the share sheet, not one drawn here - **once, ever**, remembered in preferences, because a settings screen that returns every launch is nagging and a refusal is a decision this app respects the way it respects a refused microphone. Two consequences worth naming. The two asks are **sequential**: opening the settings Activity while the POST_NOTIFICATIONS dialog was still up put a full-screen window over a question the traveller had not answered, seen on the device and fixed by moving the second ask into the first's callback. And the app **listens for the permission changing**: granting it on the S24 upgraded all thirteen alarms from a one-hour window to `window=0` with no relaunch. Refused, the alarms are still set, inexactly - a late warning beats none.
- **D094 - The Memory channel exists and says nothing.** The brief lists an evening memory prompt among the notification examples, and the channel is created with the other three. It posts nothing. The operational notifications could be built without asking anyone because **their text is the content's** - `criticalItem.title` and `instruction`, written by whoever authored the trip. The memory prompt has no such source: what it would say, at what hour, and whether it is every night or only nights with a walk is copy no approved sheet draws. Creating the channel anyway is the useful half - it appears in Android's settings from this build, so the traveller can switch it off before it ever speaks, and a channel added later is a new switch defaulted on. The question went to the design confirmation stack rather than being answered here, which is the same rule that kept the passport out of the package and the "contact with no telephone" copy unwritten.
- **D095 - The declared audio length is checked against the file, and it is an error at every stage.** `AudioGuideRequest.Playable.declaredDurationMs` is `durationSeconds * 1000`: that number, and not the recording, draws the progress bar and writes the duration label, so a guide declared at 720s over a 690s file shows a bar that reaches the end while the narration is still speaking, and one declared short shows a bar that never arrives. Nothing compared the two - a grep for "chapter" in the validator returned nothing at all. `validate_trip.py` now reads the real length out of the packaged file and checks three things: the declared duration against it, that no `chapters[].startSeconds` sits at or past the end, and that the chapter marks increase. Two formats are read, in pure stdlib and with no new dependency: the `mvhd` atom of MP4/m4a, which is what the real guides will be (`targetAudioPath` ends in `.m4a`), and WAV, which is what the repository already carries - `ag.bascarsija` declares 720 over an 8 kHz prototype that is 720.0s, and that positive case is a test. **The severity is the part worth arguing.** The offline-document check is a warning until `contentStatus` is production, and this one is not: it follows the IANA zone check and always fails. The difference is not how serious the two are but what they are about. A document declaring `availableOffline` with no file is content that *is not there yet* and will be; the promotion to production is exactly when it must have arrived. A duration that disagrees with its file is two facts that are both already present and contradict each other, and no later step in the pipeline resolves it - the number is simply wrong, and a bar that lies is a defect on a prototype for the same reason it is one in production. What is never an error is an absent check: a format this script cannot time - `.mp3` above all, which needs its frames counted - and a file not packaged yet are both reported by name as guides that were not timed, because a validator that rejects a file for being an mp3 teaches people to stop running it. The tolerance is two seconds, and the two are separate: one for a whole-second number describing a fractional file, one for the frame an AAC encoder pads at the end. Nothing real lives above that - the defect this exists for is a number typed from the wrong take or carried over from another guide, wrong by tens of seconds, and a tolerance loose enough to swallow thirty seconds would swallow the only error there is. Built now rather than after the three real guides are authored: the same check written next month guards a number that has already been wrong for a month.
- **D096 - A timeline row's affordance and its destination are one answer.** D065 made a row tappable only when it points at a screen, and then wrote that rule in three places: a set of openable kinds beside screen 02's row, a `when` in `TodayRoute`, and the same `when` again in `FullDayRoute`. All three said transport and accommodation. `walk` was in none of them, so day 9's packaged 11:00 line - `kind: "walk"`, `refId: "walk.sarajevo.historical"` - drew its hour and its marker and answered nothing, while `Routes.WALK` and screen 06 had existed since Phase 3. The only way into a walk was Explorar -> Cidade -> card, which is not where anybody stands at eleven in the morning: during the day the traveller opens **Hoje**. `timelineDestination(kind, refId)` is now the whole rule, and both routes and the row itself ask it - the tappability and the route cannot disagree again, because there is no second list to fall out of step. `attraction` stays out on purpose: screen 02's "now" block already carries "Ver atracao" for the attraction under way, so that row is not the dead line the walk was, and giving it a second door is a design change nobody asked for.
- **D097 - The first-element rule reaches the lists that are scoped to a day.** The guard written after D090 matched `content.trip.<collection>.firstOrNull()`, which is the shape the first three occurrences took, and it could not have caught the fourth: screen 17 read `day.cityIds.firstOrNull()`, a list already scoped to the day, with no `.trip.` on the line to recognise it by. It now also accuses `.first()`/`.firstOrNull()` on any property ending in `Ids` - `cityIds`, `documentIds`, `accommodationIds`, `transportIds` - because those are the lists where reading the first element is a choice somebody made: `cityIds` sits beside a declared `baseCityId`, and `documentIds` has no declared primary at all, which is the same thing said the other way. Five correct sites became offenders and all five were marked rather than rewritten, which is the point of the rule: three `baseCityId ?: cityIds.firstOrNull()` reads that are the *right* pattern still have to say that the second half is a fall, and the two `documentIds.firstOrNull()` reads on screens 15 and 16 now say out loud what nobody had written down - the schema declares no primary document, the screen has room for one, and a leg carrying two tickets shows whichever the author listed first. `AppNavigation.kt:512` is a sixth of the same shape and is **knowingly out of scope**: the rule scans `domain/` plus `*State.kt` and `*UseCase.kt`, and the navigation graph is none of those. Widening the scan to a whole feature tree to reach one correct line would trade a rule people keep for a rule people silence.
- **D098 - A coordinate is checked against the rest of its own city, and the range was never the problem.** §27 asks for "invalid coordinates" and the obvious reading was already built: `$defs/geoPoint` bounds latitude to -90..90 and longitude to -180..180, and schema validation runs first, so a range check would add nothing. What passes today are the errors that stay **inside** the range. The Latin Bridge is 43.8576 / 18.4289; transposed it is 18.4289 / 43.8576, which is Saudi Arabia and satisfies every rule in the schema. A flipped sign puts it in the South Atlantic. A transposed integer digit, 43 written as 34, changes country. Each costs a story that never fires, or one that fires where nobody is standing, and nothing in the pipeline noticed. **The anchor had to be invented, because `city` carries no `geo`** - the points are the only geography in the package. Four of the five places a coordinate can appear declare a required `cityId` (attraction, walk, story, accommodation), so they group by it, and each point is measured against the **nearest** other point in its own city rather than a centroid: with two points and one of them wrong the centroid sits between them and blames both equally, while the distance between the pair is the signal. **The threshold is 100 km and the argument for it is arithmetic.** Correct content produces 428 m - that is the span of all four packaged Sarajevo points. The three defects produce 1,001 km, 3,691 km and 9,754 km. Twenty-five kilometres would also catch a transposed *decimal* (43.8576 typed as 43.5878, which moves the point 30 km) and would sit inside the legitimate spread of a large sparse city: days 1 and 20 of this trip are Sao Paulo, whose car park is in Guarulhos, ~25 km from the centre. A guard that shouts at correct content is a guard people switch off, so **the transposed decimal is recorded as out of reach**, with its number, the way `.mp3` is recorded as a format the duration check cannot time - a 30 km error needs a map, not a validator. **The transposition is named, not just the distance**, because that turns an accusation into an instruction: when swapping the two values brings the point within 5 km of the neighbour it was far from, the finding says so and the reader knows what to type without opening a map. What the swap test cannot do is say *which* of two points is the wrong one - transposing either half of a transposed pair lands on the other half, by construction - so the subject depends on how much city there is: with three or more coordinates the wrong one is the one standing alone and is named, and with exactly two the finding names both and says there is no third point to break the tie. **Three absences are declared rather than papered over.** A transport endpoint is not checked: `transportEndpoint` declares no city, a leg's two ends are legitimately in different countries, and both ways to infer one are worse than nothing - `days[].cityIds` puts Amsterdam, Corfu, Sarande and Ksamil on one day, and a time zone is a political boundary that spans 60 degrees of longitude in China. A city with one packaged coordinate is not checked, because it has nothing to be compared with; the only cure would be a bounding box per `countryCode`, which is invented data inside a validator and is refused on the same grounds that kept the emergency telephone numbers out of the code. Both are printed by name, because an absent check said out loud is not a hole. **And a coincident pair is correct content, never a finding**: the real package's walk starts at its first stop, so `startLocation` repeats that stop's trigger, and in the sample the Latin Bridge story sits on the Latin Bridge attraction. A nearest neighbour at zero metres is the strongest agreement there is, and it has a test of its own so that nobody "fixes" it later. **Severity follows D095 exactly**: always an error, prototype/draft included. A point and its siblings are all already in the package and contradict each other, and no later step resolves it - unlike a document that declares `availableOffline` before its file has been produced.

- **D099 - The walk stop numbering is checked, and the two halves of it have different severities.** §27 asks for "bad walk ordering", and the target moved once the code was read: `WalkModeState.kt:105` does `sortedBy { it.order }`, so a walk numbered 1, 3, 2 does not break - it is **silently reordered** to 1, 2, 3 and runs correctly. What was missing is not reading, it is checking, and the two things that can go wrong are not the same kind of thing. **A duplicate is an error at every stage.** Two stops both numbered 2 leave which one the traveller walks first decided by `sortedBy` being stable - correct by accident, which is the worst way to be correct. Both stops are already in the package, they contradict each other, and no later step resolves it: this is D095's audio duration, one collection over. **A gap, or a first stop that is not 1, is a warning until production.** Stops numbered 1, 2, 4 sort into the right sequence and the walk runs; the missing 3 is content that has not arrived yet, and the promotion to production is exactly when it must have. This is D014's offline document, and the split is the same test applied twice: are these two facts already present and contradicting, or is this one fact still on its way? `stops[].storyId` is deliberately not checked here - `content_checks` already resolves it against the story registry, and a second copy of that rule is the defect D096 had just finished removing.
- **D100 - The sentence has to describe the condition the code tested, not the one the author had in mind.** D098's two-point finding printed "with no third coordinate in the city to say which of them belongs there". The condition the code actually tests is *both members of this pair are far from everything*, and three or more mutually distant points satisfy it just as two do. A city carrying Sarajevo, Paris and Sao Paulo produced two findings, each asserting there was no third coordinate, in a package that had three. The prose of D098 said the right thing - "with exactly two there is no cluster" - and the code generalised past it, which is the ordinary way a comment and a branch drift apart: the comment describes the case the author was thinking about and the branch accepts every case that reaches it. **The behaviour was correct and did not change**: with no cluster there is no way to choose a culprit, and refusing to choose is the right answer for two points and for ten. Only the stated reason was wrong. Two repairs were available - condition the old wording on `len(points) == 2`, or find one sentence true in every case that reaches the branch - and the second was taken, because a pair of wordings is a pair of things that can fall out of step again, which is the defect D096 had already removed once. The finding now says **"neither has a near neighbour in the city to anchor it"**, which is exactly what was tested, and the transposition diagnosis is unchanged behind it. The case has a test now; it had none, which is why nobody noticed the sentence was arithmetic-free.

- **D101 - The nearest neighbour asks whether a point has a friend; the diameter asks whether the city is a place.** D098 measured every coordinate against the nearest other coordinate in its own city, and that question has a blind spot with a name: **two coordinates wrong the same way become each other's friend**. Transposing two of the four packaged Sarajevo points leaves two tight groups 3,691 km apart, every single point with a neighbour 300 m away, and the check reports nothing at all - `findings=0`, `rc=0`, not one line. Transposing all four is the same silence. This matters more than it sounds, because it inverts which defect is guarded: **the transposition typed by hand is the rare one and it was caught; the transposition applied by a script or an agent to a group is the common one and it was not.** D098's own proof of failure only accused because the other three Sarajevo points happened to form a cluster - add a second transposed point to that same test and it goes green. So the city is now also measured against itself as a whole: **the distance between its two furthest points, against the same 100 km**, with no new constant, no new dependency and no external anchor. The headroom is the same two orders of magnitude on both sides - correct content spans 429 m in the sample and 296 m in the real package, a large sparse city would span ~25 km, and the blind case spans 3,691 km. **The diameter runs only where the nearest neighbour found nothing in that city**, which keeps one defect to one finding: a single wrong point stretches the diameter too, and where the nearest neighbour can name the culprit its message is the better one. That has a cost, and it is named rather than discovered: a city carrying *both* a lone wrong point and a transposed group reports only the lone point, and the group surfaces on the next run once the first is fixed. The message names the two extremes and refuses to pick, on D100's reasoning - with the city split into two coherent groups nothing in the package says which group is the city - and adds the transposition sentence when swapping either end resolves it. One limit is asserted in a test rather than left implicit: **a city moved wholesale is internally consistent and cannot be caught here**, because it is one place, and the wrong one.

- **D102 - The guide names the place, the story carries the sentence, and now something checks it.** Screens that resolve a story's audio show `story?.title ?: guide?.title` - a fallback, one or the other. **The player is not a fallback: it shows both at once.** `WalkModeController.kt:231` passes `subtitle = story.title` into `audioGuideRequest`, `AudioGuideRequest.kt:49` sets `title = guide.title`, `AppNavigation.kt:462-463` hands the pair to the compact player as `title` and `compactSubtitle()`, `PlaybackState.kt:56` returns the raw subtitle when there are no chapters, and `Media3AudioEngine.kt:70-73` puts them in the media session as `setTitle` / `setArtist` / `setSubtitle`. So two equal titles print **the same line twice**, in the compact player and in the media notification - which is the lock screen, which is Walk Mode with headphones and the screen off, which is the single scenario the walk was designed around. The three Sarajevo guides shipped that way: `title` identical to the story's, promoted into the APK, and **no tool caught it** - it was found by review reading the player chain. *(Corrected on 2026-09-05, when the guard ran for the first time: this paragraph first said the origin was an instruction that told the author to use the same title and asserted that no screen showed both. That instruction was indeed wrong, but it is not the origin - it stops one step short. **The instruction copied the pattern out of `sample-trip.json`, where `story.latin-bridge` and `ag.latin-bridge` had both been titled "Latin Bridge" since the bootstrap**, and that file is the artefact this repository uses to teach how content is written. So the defect was not introduced by the three guides and was not introduced by the instruction: both copied it faithfully from the example. The guard did not catch only the copies, it caught the source - it accused the two tracked packages on its very first run, and the string was fixed there in the commit after this one, `ag.latin-bridge` becoming "Audioguia da Ponte Latina" and joining the convention its two sibling guides already followed. A record that stopped at the instruction would leave whoever reads it tomorrow believing the example was right.)* The guard exists so that neither the instruction nor the example has to be right next time. This is the third defect in this series to earn a guard after being found once, beside the first-element rule (D097) and the audio duration (D095). **The severity is the part worth arguing, because it is neither of the two established shapes.** It is not D095's contradiction - the two titles agree, and the agreement is the defect. It is not D014's absent content - nothing is missing. What decides it is that a guide title copied from its story is a *plausible intermediate state* while the real title is still being written, and the promotion to production is exactly when it must have been: that is D014's shape after all, and the evidence was in front of the check on its first run. So it lives in `content_checks`, a warning until the package declares `production` and an error inside it. Read from the story side, so a guide nobody points at has nothing to collide with and never enters, and compared stripped and case-insensitively, because a title retyped rather than copied prints the same duplicated line.

- **D103 — The geofence is an alarm clock, not a judge, and the floor is 120 m.** Passive story discovery had one honest way to fail and one dishonest one, and the dishonest one is the interesting half. The three real triggers declare `radiusMeters: 80`. Registering an Android geofence at 80 m is the honest failure: the platform promises nothing at that scale, latency grows as the radius shrinks, and the result is a circle that *sometimes* does not ring — which is worse than no circle, because nobody can tell the difference between "no story here" and "the story failed to fire". The dishonest fix is to raise the content's radius to something the platform can watch, and it would have worked in the emulator and been wrong in Baščaršija: the three Sarajevo triggers are 260–380 m apart, so an 80 m circle widened to 120 m starts announcing the Sebilj from the far side of Ferhadija, and "você está aqui" becomes a lie in the one place this app has to be trustworthy. **So the two numbers are separated instead of reconciled.** `GEOFENCE_FLOOR_RADIUS_METERS = 120f` is a *registration* number and never reaches a decision: the content's 80 m plus the ~40 m a fused fix carries in a dense old town, which is the smallest ring that can be trusted to ring. When it rings, the position delivered with the wake-up goes through `decideStoryTrigger` at the trigger's own 80 m. **`Nothing` is the expected answer there, not an error** — the outer circle exists precisely so that it will sometimes be too early. Which is the whole point: nothing outside `decideStoryTrigger` compares a distance with a radius. D096's defect is a rule written twice, and widening the radius is the shape that would have reintroduced it *while passing every test*, so the floor lives alone as a `const` with `registrationRadiusMeters` beside it, commented the way `COORDINATE_CLUSTER_LIMIT_METERS` is in the validator: a tuning number that is not a rule must not be mistaken for one. One consequence is named rather than discovered — crossing the outer rim at 100 m and then walking to the plaque produces no second ENTER — so `GEOFENCE_TRANSITION_DWELL` is registered alongside ENTER with a one-minute loiter: a second ring of the same alarm, not a second decision. `ACCESS_BACKGROUND_LOCATION` returns to the manifest on exactly the terms D033 removed it under: it is used now, by this, and by nothing else. Play Services geofencing is also why any of this may exist outside a walk — the system watches the circles with the location it is already spending, so `LocationSource`'s line that high-accuracy GPS must not run outside Walk Mode is untouched.

- **D104 — A refused background permission is answered once, on screen 19, and costs one thing only.** From Android 11 the system shows no dialog for `ACCESS_BACKGROUND_LOCATION` at all: requesting it returns a refusal the traveller never saw, and the only real path is Android's own settings page. That is D093's situation again — a permission the app can point at and never grant — and it gets D093's posture: **asked in one place, never as an interruption, and never twice.** A row in "Viagem e aparelho" on screen 19, drawn with the components that section already uses, appears only while passive discovery is off and is gone the moment it is on; it says what the permission is for and, in the same breath, that refusing it changes nothing else: *"Sem ela nada muda: o passeio, os áudios e as histórias da cidade continuam iguais."* That sentence is true and is the reason the row can afford to be quiet — it is `LocationSource`'s own posture, "a refused permission costs the traveller automatic stories, never the walk and never the audio", said to the traveller instead of only to the reader. Nothing about the walk, the audio, screen 04's stories or the trigger record depends on the answer. Android 10, where the system does still ask, gets the dialog from the same row.

- **D105 — Where a passive story opens, what happens during a walk, and how many circles are registered.** Three questions the design left open. **(a) The notification opens screen 04, Cidade — and this is a proposal, not something found in the prototype.** The approved prototype was read first: screen 10 appears once, in `Fluxo Principal`, drawn *over the dark walk field* — "4 de 7 · ≈ 28 min restantes" is part of the composition, and its own footnote is "Um toque suave avisou nos fones. O celular pode continuar no bolso." It is a sheet over screen 07, and outside a walk there is no screen underneath it. **The prototype draws no out-of-walk story surface at all**, so the answer is not in it. What *is* in it is screen 04's "Histórias curtas" section — the same story, with its own *Ouvir* and *Ler* — and in the real package all three Sarajevo triggers sit on `city.storyIds` for Sarajevo, so they are already on that screen. The tap therefore lands on Explorar, through the same `EXTRA_ROUTE` a deadline already uses (D092), and no screen was invented. The limitation is stated because it is real: `CityRoute` resolves the city from the day, so on day 14 — which starts in Sarajevo and sleeps in Mostar — a tap opens Mostar. A per-story destination would fix it and is a design question that was not asked. *(Seen on the emulator on 2026-09-06, before the trip starts: the Sebilj notification opened Explorar correctly — the tab highlighted, the route consumed — on São Paulo, which is day 1's city. The route is right; the city is the day's, not the story's.)* **(b) Passive discovery is silent while a walk is running.** `WalkModeController` loads the trigger record into memory at `start()` and writes through it; a record written behind its back would not be seen by that copy, and the same story could speak twice — once from the geofence, once from the walk. The walk owns the decision while it runs, which is also what §20 says: "current route context may override passive geofence behavior". The circles stay registered throughout; only the answer is suppressed. **(c) All three circles are registered, unfiltered — and here is the cheap filter that was rejected.** It exists: `story.cityId` against today's `day.cityIds`, both already in the content, about three lines. It was not applied, for a reason stronger than the 100-per-app ceiling that three circles cannot approach. Registration happens when the app is on screen and after a boot; there is no daily background job, and inventing one is the infrastructure this was told not to invent. A traveller arriving in Sarajevo on day 12 from Bastaši, with the app last opened the evening before, would have **nothing** registered for Sarajevo and would walk through Baščaršija in silence until they opened the app — which is precisely the moment passive discovery exists for. A dormant geofence 2.000 km away costs nothing measurable; a filter that can be stale costs the feature its only scenario.

- **D106 — The approved primary action is full width at 56dp, and a `FlowRow` had quietly demoted two of them.** Screens 10 and 11 each draw one action that matters more than the others, and the approved sheet gives both the same shape: *"**Ouvir agora** (56dp, teal), *Ler*, *Depois*"* and *"**Gravar memória** (teal, 56dp) e *Voltar para Hoje*"*. The prototype draws them literally — `width:100%;min-height:56px;background:#1F6F78` with an icon, and the secondaries beneath on their own row at `flex:1;min-height:52px`. Both screens instead put every action into one `FlowRow` of auto-width 52dp buttons, so the primary came out **118dp wide on screen 10 and 150dp on screen 11**, in both cases *narrower than the secondary beside it* and 4dp short of its declared height. Nothing measured it before because nothing had measured these two screens at all: the first visual pass covered eight and neither was among them. **The `FlowRow` was not carelessness and that is the interesting part** — it was put there deliberately, citing D066 and D072, to stop two long labels being squeezed to one character per line at 360dp with the font at 1.5. The reasoning was right and the remedy was aimed at the wrong layout: the approved composition is *already* immune, because the primary owns the full width and *Ler* and *Depois* get half of it each — 153dp apiece at 360dp, more room than the three-across row ever gave them. Re-measured at 1.5 after the change: 316 × 56, 153 × 52, 153 × 52. The lesson is narrower than "don't use FlowRow": **a defensive layout is still a layout, and it has to be checked against the sheet like any other.**

- **D107 — The same line twice, one screen over from where it was first found.** D102 found a guide titled exactly like its story printing the identical line twice in the media notification, and put a guard in the validator. Screen 10 was doing it in Compose, and no guard could see it. Its operational line is drawn as *distance · place · audio length* — the sheet's own "A 20 m de você · **Ponte Latina, margem norte** · 6 min de áudio" — and the middle field read `stop?.title ?: city.name`. **A walk stop's title is `story.title` by construction**, assigned in `prepareWalk`, so the field could only ever be the headline again: with the real package the sheet printed "O Sebilj tem 1891; a praça tem 1462" as its h1 and then, two lines below, "a 0 m · O Sebilj tem 1891; a praça tem 1462 · áudio de 3 min". The fallback that was supposed to be the specific answer was the one thing that could never be an answer at all. Worse than the code: **the unit test asserted the duplicate.** `StoryTriggerStateTest` had `assertEquals("a 40 m · Latin Bridge · áudio de 9 min", …)` beside `assertEquals("Latin Bridge", state.title)` on the next line, written and reviewed and green — the example teaching the defect, exactly as `sample-trip.json` had taught D102's. The stop is not consulted now and the city is the place; the test asserts the *rule* rather than the string, that the line never contains the title. What remains is a content limit, stated rather than fixed: no field in the schema carries "margem norte", so "Sarajevo" is the most specific place the app can name, and inventing a field is a content decision this pass did not have to make.

- **D108 — Three hairlines the real package made visible, and one of them is on the emergency screen.** Small, and grouped because they are one habit: a card drawn with `background(Surface)` and no border, on paper. The approved sheets draw borders. *Screen 11's four stat tiles* had none, and with the real package "HISTÓRIAS OUVIDAS" wraps to two lines while "QUEM OUVIU" does not — so the pair also came out **ragged**, one tile a line taller than its neighbour, where the approved grid is `grid-template-columns:1fr 1fr` and stretches by default. Both are fixed: `Neutral200` at 1dp, and `fillMaxRowHeight`, which resolves from the row's own measured height and so is not D051's or D066's zero-under-an-unbounded-constraint shape. *Screen 17's 122 and 124* were missing something sharper — `border:2px solid #16232E`, **2dp of ink**, the only place in the app the sheet asks for a border that heavy. That is not decoration on the screen read in a hurry and possibly in sunlight; it is the contrast that separates the police and ambulance numbers from the paper. Measured after the fix: 4px of `#16232E` at 2px/dp. None of the three is visible in the sample package, and none would have been caught by a test.


- **D109 — The retention note is a sentence about mock content, and a real package that simply has no number is a different state.** `WITHHELD_NOTE` — "O número chega com os dados reais da viagem." — was written for D061/D064's problem: the package ships `+000000000` for the insurer and `+387000000000` for the host, a button that dials one of those fails at the moment it is pressed, and the row stays to say *what is missing and when it arrives*. In the package that actually boards (`assets/trip-production`, `isMockContent=false`) **five of the ten stays carry no `contactPhone` at all** — acc.bastasi.camp, acc.sarajevo.estudio, acc.dubrovnik.cidade-velha, acc.cilipi.estudio, acc.amsterdam.amigo, covering **days 11, 12, 13, 16, 17, 18 and 19**. A camp and a friend's spare room are not placeholders waiting to be replaced; nothing is arriving. `phone()` collapsed both states into one `withheld` flag, so screen 17 printed a promise that was false, **on the safety screen**, on seven of the twenty days. The gate is now split: withheld-as-mock keeps the note, a blank number in a real package is undialable and says nothing. And screen 17's own header states the rule the row broke — *everything on it is either a number to call or a sentence to show a stranger* — so with no number there is no row, the `contactPhone?.let` **screen 16 already used** (`StayState`), reached for here. No new copy: the approved sheet does not draw "sem telefone cadastrado", and the emergency screen is not the place to invent a string. Mock behaviour is unchanged, which is why every existing test stayed green without being touched.

- **D110 — The release is signed with the debug key, on purpose, and the DEX check starts meaning something.** `app/build.gradle.kts` declared no `buildTypes` and no `signingConfigs`, so `assembleRelease` produced **`app-release-unsigned.apk`, which does not install**. Everything that followed from that was worse than the missing line: device QA in IMPLEMENTATION-STATUS was run "against a debug build", and it is the debug build that is on the telephones — the one that compiles `app/src/debug/.../WalkArrivalScaffold.kt` and draws "Protótipo · simular chegada" on screen 07. The guard we run on every pass, *release DEX contains no "Protótipo"*, was proving a property of an artefact **nobody could put on a phone**. One line fixes it: `signingConfig = signingConfigs.getByName("debug")` on the release build type. The debug key is the right key here and not a shortcut — this app is side-loaded by ADB onto two telephones, is never distributed, and a fresh keystore would be one more secret to carry to Europe for no gain; if it is ever published, that is the moment to make a real one. **No `isMinifyEnabled`**: shrinking would buy a few hundred kilobytes and cost the readability of every stack trace from a trip with no debugger attached. The binary for device QA is now `app-release.apk`.

- **D111 — Room stays at version 1 with no migration and no destructive fallback, and the consequence is written where it will be read.** `MemoryDatabase` declares `version = 1`; `TravelCompanionApp` builds it with neither a `Migration` nor `fallbackToDestructiveMigration()`. The first change to `MemoryEntity` that ships without a migration makes Room throw on open — *"Room cannot verify the data integrity"* — in the builder, which means **the app does not start**, with the trip's voice memories inside the database it will not open. That is a real trap seven days out, and the fix is not to change the strategy but to stop it being a surprise: a KDoc on `@Database` states it. **`fallbackToDestructiveMigration()` is refused explicitly**, because it is exactly the fix someone reaches for under pressure and it is the worse one — it converts the crash into a silent `DROP TABLE`. A voice memory is the only thing this app holds that exists nowhere else: not in the package, not in Firebase, not on paper, and it cannot be recorded again. Failing to open is recoverable by shipping a migration; deleting is not. `exportSchema = false` stays for now and goes to `true` in the same commit as the first migration, since there is no schema history to diff until one exists.

- **D112 — Two `Log.i` sites, and deliberately not a logging library.** Until now the app had no `Log.*` call anywhere. That is defensible for a product with no console, and indefensible for the two subsystems whose failures are **silent and time-critical**: if the 04:45 bag drop on day 18 does not ring, nothing distinguishes "scheduled exact", "scheduled inexact and held to a maintenance window" (D093) and "never scheduled"; if no story speaks in Baščaršija, nothing distinguishes "no circle was ever asked for" from "asked and Android stayed quiet". Both were unanswerable from outside the app, and both are answerable with one line each. So: `CriticalAlertScheduler.schedule()` logs one line per alarm (id, fire instant in the phone's own zone, exact or inexact) plus a total with `canBeExact()`; `PlayServicesStoryGeofences.register()` logs one line per circle (story id and radius), one line naming *which* permission is missing when it returns early, and one when the call throws. **`Log.i` and not `Log.d`**, because `Log.d` is what a release build is expected to strip and the release APK is the one going on the trip (D110). §33 leaves "exact logging library" open; this closes it with *none* — a library would be a dependency, a configuration and a new failure mode seven days out, to replace `adb logcat -s TravelCompanion`, which already answers the two questions. **What never enters the log:** no telephone number, no address, no participant name, no continuous GPS position — the two subsystems that could leak them are precisely these two, so the restraint is stated where it is tempting to break. **One honest limit:** `addGeofences` accepts or declines asynchronously, and reading that answer means a callback, which is a restructure this pass refused. The per-circle line therefore says *requested*, never *registered*, and a Play Services decline still goes unlogged. Named here rather than papered over.

- **D113 — A host's telephone enters the package only if a document prints it, and five of them do not.** The five stays with no `contactPhone` — Bastasi, Sarajevo, Dubrovnik, Čilipi, the friend's room in Amsterdam — were taken back to the vouchers in `source/private/` to be filled, and **none of them could be**. The reason is a pattern rather than an oversight: the five stays that *do* carry a number were booked through Booking.com or Decolar, whose vouchers print the property's line beside the address; the three that do not are **Airbnb**, whose trip sheet gives the host's first name (Dino, Teo, Mihaela), a *"Ligar para o anfitrião"* button and no number anywhere — not in the text, and not as a `tel:` link annotation behind the button, which was checked directly in the PDF objects. Bastasi and the friend's room have no document at all: no file in `source/private/` so much as mentions Bastasi, rafting or the Tara. So the field stays absent, deliberately. **An absent field beats a wrong number**, because a number invented from a property name fails at the one moment it was kept for (D064) — and since 13f2f75 an absent field is no longer a false promise either: screen 17 draws no row rather than "o número chega com os dados reais da viagem" (D109). The cost is stated plainly: on days 11, 12, 13, 16, 17, 18 and 19 the emergency screen offers no accommodation line, and closing that gap needs a number from the traveller, not from this repository.

- **D114 — A note on an emergency contact describes the contact it is attached to.** Montenegro's profile carried, on `generalEmergency` — the 112 button — the sentence *"Montenegro não tem posto brasileiro; quem cobre é Belgrado, por jurisdição cumulativa."* That is a fact about the consulate, filed against the ambulance-and-police number, and it also restated the `consular.label` standing two fields below it ("Embaixada em Belgrado"). The schema already allows `note` on any `emergencyContact`, so it moved to `consular`, reworded to stop repeating its own label: *"Montenegro não tem posto brasileiro próprio; esta embaixada o cobre por jurisdição cumulativa."* The rule this sets is narrow and worth holding: a `note` qualifies the number it sits on. **Bosnia's `generalEmergency.note` was deliberately left alone** — it claims the screen shows "122, 123 e 124" when the schema has no field for a fire brigade and the profile carries only 122 and 124, and that is a second defect whose fix depends on a design decision nobody has taken. No `generalEmergency.note` reaches a screen today, so neither is visible; this one is now merely *true*, which is the condition for showing it later.

- **D115 — A day named in prose is a day number, never a date.** Day 3's outfit line read *"O casaco sai de cena aqui e só volta a servir em Žabljak, no dia 20"*, on a screen whose own header reads **"Dia 3 de 20"**. The "20" was the date — 20 September — and the screen offered it to a reader already counting days, on which reading the sentence points past the end of the trip. It was wrong twice over: Žabljak is **Dia 8**, and the coat comes back a day earlier than that anyway, on **Dia 7**, whose own line already says "Casaco fino mesmo com 27 °C na baía" for the climb to the Lovćen massif. Corrected to *"…só volta a servir na subida ao Lovćen, no dia 7"*, which makes the number and the fact true together. The class is the one D107 and D102 keep finding — a field that reads plausibly and says something the rest of the package contradicts — and the rule is cheap to hold: **in prose, `dayNumber`; dates belong in `date`.**

- **D116 — The detailed itinerary is a source the vouchers are not, and it overturns four fields.** The traveller's own detailed itinerary — 1808 lines of prose, held outside the repository because it carries booking locators, door PINs and card references — was read against the packaged trip for the first time. It is not a voucher and it is not `source/itinerary/`: it is the document the traveller writes *from* the vouchers and the e-mail threads, and it therefore holds facts no voucher ever printed. Four corrections follow, and each one names the reason the package was wrong rather than merely the new value. **(1) The coat goes back to Žabljak, reversing D115's second half.** D115 was right that the "20" was a date offered to a reader counting days, and right to replace it; it was wrong about where the coat returns. It reasoned from Dia 7's own outfit line — *"Casaco fino mesmo com 27 °C na baía"* — and inferred that the Lovćen day was the first the coat is wanted. The itinerary settles it directly, and in the traveller's words: the Lovćen day is *"o dia mais leve para vestir da viagem inteira"*, and it is the next day, in Žabljak, that reads **"O casaco sai da mochila aqui"**. A `carry` line naming a thin jacket for altitude is not the same claim as the coat coming out of the pack, and the source outranks the inference drawn from a neighbouring field. Now: *"…só volta a servir em Žabljak, no dia 8"*. D115's rule stands untouched — in prose the day is `dayNumber` — and only the destination changes. **(2) The camp's telephone existed, outside `source/private/`.** D113 recorded that no file in `source/private/` so much as mentions Bastasi, rafting or the Tara, and that was accurate: the camp issued no voucher, so no voucher could print its line. The number reached the traveller by e-mail and landed in the itinerary's day-23 contingency, where two are given. `acc.bastasi.camp.contactPhone` is now **+381 64 420 1956**, with the second, **+381 65 475 9112**, in `instructions` as the fallback. This does not soften D113's rule — a number still enters the package only if a document prints it — it widens what counts as a document, and it closes the gap on the most isolated base of the route, where screen 17 had no accommodation row at all. Four stays remain empty and remain empty for D113's reasons. **(3) Podgorica's 4h38 was invisible.** `transport.ksamil-budva.night` is modelled as a single leg, 16/09 19:30 to 17/09 11:07, and the whole of the night's difficulty sat behind one clause of a timeline `detail`: *"Troca em Podgorica de madrugada"*. The itinerary details what that means — arrival **05:00**, next boarding **09:38**, a second leg run by a different company (Touring Kotor, route CHR3229, **carrying no FlixBus livery**), and station fees payable only in cash. The leg is not split, because it is one booking and one reference; instead the connection gets a `criticalItem` with `actionByTime` **09:20**, and Dia 5 gets a timeline row at **05:00** so the wait is a thing on the screen rather than a subordinate clause. **No `timeZone` override is needed**: both times are `Europe/Podgorica` and Dia 5 declares that zone; the row would have needed one only on Dia 4, which declares `Europe/Tirane`. *(Imprecise, corrected on 2026-09-07 by D140: that sentence reasons about the **timeline row** and never about the **critical item**, and the item was the half that was wrong. Hung on `transport.ksamil-budva.night`, which is named in `transportIds` of **Dia 4 alone**, the connection was collected by `criticalItemsFor` on Dia 4 and fired as `2026-09-16T09:20` in `Europe/Tirane` - the right wall clock, the wrong day, twenty-four hours early. What it needed was not a zone override but a different owner. The item now lives in Dia 5's own `criticalItems`, and the conclusion holds for the item as well as for the row: no override, because the day it belongs to already declares the zone it is written in.)* **(4) The return flight is OU661, not OU 663.** The package said `OU 663` in six places; the itinerary says **OU661** twice, with the same booking reference and the same times, so the two could not be reconciled by reading either of them. The tie was broken by opening the ticket itself — `Croatia Airlines Ticket.pdf` in `source/private/` — whose flight table reads `30SEP 0615 DUBROVNIK ZAGREB 0710 OU661`. All six occurrences are corrected, **including the transport's id**, which embedded the wrong number: nothing outside the package referenced `transport.dubrovnik-zagreb.ou663`, and leaving it would have left the wrong number in the one place a later reader would grep.

- **D117 — A `usefulApp` row is an install link, so `requiresInternet` stays true on all seven.** `usefulApps` was empty and screen 19's "Na estrada" section drew nothing for it; the itinerary's *Apps e ferramentas* section now fills it with **seven** entries, in the priority order the document itself sets. Three choices are worth the ink. **(1) A store *search*, never a package id.** The itinerary names each app and never its identifier, so `uri` is `market://search?q=<the name the document uses>&c=apps` with the `https://play.google.com/store/search` form as `fallbackUri` — `ExternalActionLauncher.open` tries the first and falls through to the second, so a phone without the Play Store still lands somewhere. A package id would have to be supplied from outside the document, and a wrong one is a dead end at the exact moment the row is pressed. **(2) `requiresInternet: true` on every one of them, including the two apps that work offline.** This is deliberate and it is the opposite of what the section is *about* — the document's own headline is that 13 of the 15 nights have no European roaming — so the reasoning matters. `requiresInternet` documents **the action**, not the app: `TripModels.kt` says so where the default is declared, *"assume an action needs the network unless the content package states otherwise, so offline UI never over-promises"*, and the one place the repository writes `false` is `directionsAction`, whose primary `uri` is `google.navigation:` — an installed app opened with no network. A Play Store listing is not that. Every action here is an **install**, which the document itself frames as a thing to do "antes de sair de casa", and an install needs the network whoever is asking. Marking it `false` would be precisely the over-promise the field exists to prevent. So the offline fact goes where it is both true and *visible*: into `name`, which is the only string screen 19 renders — "Organic Maps ou Maps.me · **mapa offline dos seis países**", "Google Tradutor · **pacotes de albanês, bósnio, croata e grego**". A flag no screen reads today would have carried it nowhere. **(3) The bank app is the one of the eight left out.** The document's eighth item is *"App do banco e do cartão, com o limite de saque conferido antes"* — it names no bank, no app and no identifier, and `action.uri` is required by the schema. `market://search?q=app%20do%20banco` is not a search anyone would run. Its real content is a **reminder** tied to Kotor on days 5 to 7, and `usefulApp` has no field for that; it is listed with the rest of what schema 1.1 has no home for. `countryCodes` is set only where the document restricts — the translator's four language packs plus Montenegro, which the document says the Bosnian or Croatian pack covers; the Balkans for WhatsApp and the two bus apps; Greece and Albania for Ferryhopper, which is where the one ferry runs. **`dayIds` is set nowhere**: the document ties no app to a day.

- **D118 — Twenty-seven attractions, no coordinates, and a `refId` that is correct data without navigation.** `attractions` was `[]` in a package covering 19 cities, so screen 04 listed no attraction and screen 05 was unreachable. The itinerary's day sections now fill it with **27**, linked from `city.attractionIds`. Four choices carry the weight. **(1) No `location.geo` anywhere.** The itinerary carries map *queries* — the Google search strings behind its own "Mapa →" links — and never a latitude and longitude. A coordinate would therefore have to come from outside it, and the validator measures every packaged point against its own city's cluster and diameter (D098/D100/D101): a wrong one is caught, and a *plausibly* wrong one is not. So `location` carries `name`, sometimes `address`, and always `mapsQuery`, which is the string the itinerary itself wrote and also what `AttractionState.directionsAction` falls back to when there is no `geo`. The validator now reports one city cluster checked and **zero points with nothing to anchor them** — the same as before this commit, which is the point: 27 attractions added no coordinate risk at all. **(2) The eleven `refId`s are correct data and change no navigation, deliberately.** Eleven timeline rows were `kind: "attraction"` with `refId: null`; all eleven now name their attraction. **The rows stay unclickable.** `timelineDestination` in `AppNavigation.kt:151` routes transport, accommodation and walk and returns null for attraction, and `TimelineDestinationTest` asserts that on purpose. What the `refId` does buy today is real but smaller and worth naming precisely: `CityState.kt:104` builds its schedule map from exactly these rows, so screen 04 now prints the hour beside eleven attraction cards — "Muralhas de Kotor · 06:15" — which it could not do while the rows pointed at nothing. The navigation itself waits on a code change this content session does not make. **(3) Where the document hesitates, the field hesitates.** Kotor's walls: *"As fontes divergem entre €8 e €15 por pessoa, e sobre aceitarem cartão; com uns €30 trocados no casal a dúvida não importa"* — the itinerary's own sentence, not a number picked from it. Kravice contradicts itself, ~€5 per person in the description and €10 at the gate in the tour voucher, and `price` says so and says which to carry. A duration given as a range — 8 to 10 hours on the Bobotov Kuk, 45 to 60 minutes at the Tunnel — sets **no** `recommendedDurationMinutes` at all, because that field renders as "Visita ~N min" and any N would be this session choosing. Only a single figure the document states — 180 for Butrinto, 90 for the Black Lake, 210 for the rafting — becomes one. **(4) Corfu gets no attraction, and Sarajevo's walk gets a start.** The itinerary describes Corfu as a port to pass through — which terminal, not to confuse it with the old one — and that is transport, already modelled on the ferry leg; inventing a sight there would be the one thing this session must not do. And `walk.sarajevo.bazar-ao-rio` had no `startAttractionId`, so screen 05 could never show its departure strip; it now names Baščaršija, which is the `02 → 05` flow D008 describes. `historySections` and `interestingFacts` are written only where the document carries real history, and **neither reaches a screen today** — `AttractionState` reads `whatToObserve` and not those two. They are packaged as content, not as a promise.

- **D119 — Twelve plan Bs, four restaurants, and a date defect that turned out to have four more instances.** The itinerary's *Planos B e contingências* table lists ten rows; six had no `planB` in the package, and they are the six added here, taking `planBs` from 6 to **12**. Five hang off the thing that fails — the easyJet leg, the night bus, the train, the return flight, and the Nevidio attraction — and four also off a day whose `planBId` was free. **The ferry cancellation hangs off nothing, and that is a finding, not an oversight**: the Corfu → Sarandë crossing is a `timelineItem` with `refId: null` and no `transport` entity, and Dia 3's single `planBId` is already spent on the Ksamil check-in. Screen 19 lists every plan B in `planBs` regardless of what points at it, so it is reachable; what it lacks is a route in from the leg it belongs to, and closing that means modelling the ferry as a transport, which is more than a content session should do. **The Nevidio plan carries the first `kind: "phone"` action in the package** — the operator's WhatsApp, which `PlanBScreen.kt:163` turns into a dial row — because the document's instruction for that contingency is literally "falem com o Dane assim que a previsão virar", and a plan whose first step is a telephone call should offer the telephone. **`restaurants` gets exactly the four places the document names**: Morića Han and Zlatna Ribica in Sarajevo, Forza Kuk and Monte 1350 on top of the Lovćen. The itinerary also carries a *Comida* table of dishes and prices by country, and **none of it became a restaurant** — it is general knowledge about a cuisine, not a place with a door, and turning it into rows would have invented four cities' worth of establishments. **Two `scenario` strings were rewritten** because they read badly as the row label screen 19 draws: the Bobotov Kuk's said "travessia" for a route the same package describes as out-and-back, and the kayak's said "falta de participantes" where the document says a minimum of six. One `reassurance` was **wrong**, not merely weak — it promised "troca por caminhada guiada", which the operator's voucher does not offer; the document says another date or time, or a full refund, and now so does the field. **And the D115 rule found four more violations of itself.** Sweeping the package for `dia N` in prose turned up a date written as a day number in `acc.sarajevo.estudio` ("o código é liberado no dia 22" → **dia 10**), in `acc.mostar.guesthouse` ("as 21:00 do dia 26" → **dia 14**), and twice in the kayak plan B ("a manhã do dia 29" → **dia 17**). Three of those five numbers are **greater than 20**, so on a screen headed "Dia N de 20" they cannot be read as a day at all — which is what makes this class worth sweeping for rather than fixing one at a time. A fifth was mine, written in phase 2: the Kotor walls' `bestTime` said "o nascer do sol no dia 18 de setembro", correct but still a number a reader is counting days against, and the sunrise hour was the content, so the date is gone. **One dangling reference was found and fixed on the way**: `planb.ksamil-checkin.relatedDocumentIds` named `doc.ticket.ferry-corfu-sarande`, which has never existed — the ferry is packaged per passenger, `.vinicius` and `.erika`. `PlanBState` resolves that list with `mapNotNull`, so the row did not fail, it silently disappeared from the Plan B screen. `content_checks` validates `documentIds` on days, transports and stays but **not** `relatedDocumentIds` on a plan B, which is why six validators passed over it; the gap is named here because closing it is a change to `tools/`, which this session does not touch.

- **D120 — The launcher label is "Bálcãs"; `applicationId` and `namespace` stay `com.travelcompanion.app`.** `app_name` went from "Travel Companion" to **Bálcãs**, and that is the whole change: the string is read in exactly one place, `AndroidManifest.xml` `android:label`, and no screen displays it. **The application id is deliberately untouched, and the reason is not cosmetic.** Android identifies an installed app by its `applicationId`; changing it does not rename the app, it installs a *second* one. The two telephones that leave on Dia 1 would end up with both, and everything the old id owns would be stranded inside it — the Room database holding the voice memories, which is the only thing this app stores that exists nowhere else and cannot be recorded again (D111), and the DataStore holding which traveller this phone is (the answer to *Quem é você?*). Renaming a package a week before departure to change a word under an icon would be trading the trip's irreplaceable data for a label. So: `namespace` and `applicationId` stay, `versionCode` 1 and `versionName` 0.1.0 stay, the repository and the product stay Travel Companion, and only the launcher label is Bálcãs. The four notification channels keep their own names — they answer "what is this notification", not "what is this app". `ExternalActionLauncher`'s comment still calls the product Travel Companion and is still correct.

- **D121 — The icon art is full-bleed in the `background` layer, and the mask crop is accepted with its cost measured.** `docs/design/app-icon-source.png` is a 1254×1254 watercolour whose subject — a green backpack with terracotta straps, an enamel mug, a sprig and a bedroll — occupies **89% × 90%** of the frame. An adaptive icon is 108dp, of which only the central **66dp (61%)** is guaranteed on every launcher. **The subject does not fit the safe zone, and no arrangement of this single image makes it fit.** Path A was chosen knowingly: the whole square goes in as `<background>`, the mask cuts what it cuts, and `<foreground>` is `@android:color/transparent` because `AdaptiveIconDrawable` requires the element. **The art is in the background and not the foreground on purpose** — several launchers parallax the foreground layer, and translating a bleeding image would expose the layer behind it along one edge. **What it costs, measured rather than predicted:** against the 72/108 visible circle the mask discards about **39%** of the subject's pixels, and on the device it removes the top of the pack's lid, the outer end of the upper-left shoulder strap, both side edges of the pack body, and roughly the lower-left half of the rolled blanket. The blue field the art was composed on survives only as a sliver at the lower left; the yellow halo survives as two slivers. **The composition is gone and a close-up remains** — it reads as a backpack seen too close, not as a backpack sitting in a field. It is recognisable at 96px and above, becomes a green-and-orange mass at 48px. One thing checked and clear: the source has transparent corners rather than a painted vignette, but the minimum alpha inside the visible circle is 233 of 255, so nothing shows through. **Path B stays available and is what the lint output is quietly arguing for**: `IconLauncherShape` fires five times, once per legacy density, saying a launcher icon should not fill every pixel of its square — which is the same sentence as "89% × 90% against a 61% safe zone", written by a tool. Path B is the art delivered in two pieces, a transparent-background foreground sized to the safe zone over a plain or gradient background, and it also unlocks `<monochrome>`, whose absence is the other two new warnings. That is a commission, not a crop, and this session does not invent one. **The 3.2 MB master is versioned in `docs/design/` on purpose**, as the only large binary in the repository: every packaged density is derived from it by a five-line resize, the trip's own content package is gitignored and cannot hold it (D028), and a design system whose master art lives only on one laptop is one disk failure from being unable to regenerate its own icon.

- **D122 — The icon art was recomposed to fit the adaptive geometry, the legacy PNGs are gone, and the themed layer keeps its detail in the alpha.** D121 accepted a crop it had measured but reasoned about from the wrong premise. An adaptive icon does not simply mask a 108dp square: the launcher **first discards the outer third** — the guaranteed area is the central 66dp of 108 — and only then applies the OEM mask to what is left. The first art put the subject across 93% × 89% of the frame, so the silhouette was not trimmed at the edges, it was *removed*: what reached the launcher was a close-up of straps and canvas. **The fix is composition, not code.** The replacement art keeps the same painting and the same palette and puts the backpack in **63% × 60%** of the frame, inside the guaranteed circle, with the yellow halo as a ring around it and the blue field as the bleed the mask is welcome to eat. Measured on the packaged 432px layer, **99.86% of the subject survives the 72/108 circle and 100% survives a squircle**, against roughly 71% before; what the circle still takes is **68 pixels** at 432px — 0.036% of the frame — grazing the outer edge of the left shoulder strap in the top-left, which is invisible on the device. The bleed now costs the blurred corners and nothing else, which is what path A was supposed to cost all along. **The five legacy `ic_launcher.png` are deleted, and that is safe for a stated reason**: `minSdk` is 26, `mipmap-anydpi-v26/` therefore resolves on every device that can run this app, and lint's own `ObsoleteSdkInt` says the `-v26` qualifier is redundant *because* the minimum is already 26 — the same fact, read as licence rather than as a warning. It was verified rather than assumed: with the PNGs removed, `aapt2 dump badging` still reports `application-icon-{120,160,240,320,480,640,65534}` all resolving to the adaptive XML. Those five files were also the sole source of the five `IconLauncherShape` warnings, since a full-bleed square is exactly what that check exists to catch on a legacy icon. **The `<monochrome>` layer carries its shape in the alpha channel, not in opaque white**, and this is the detail that decides whether a themed icon is a picture or a blob: the source is 1024×1024 LA, 82.8% fully transparent, 5.3% fully opaque and **11.9% partial**, with the interior detail — strap edges, buckles, the sprig, the bedroll spiral, the mug — drawn as *holes*. The system tint therefore paints the solid mass and lets the holes read as the light field behind, and on the device the silhouette shows every one of those elements instead of a filled shape. The silhouette sits in 47% × 56% of the frame, centred, with **0.00%** outside the guaranteed circle. **`mipmap-anydpi-v26` keeps its qualifier** and `ObsoleteSdkInt` stays at 1, accepted: `-v26` is the universal convention and the folder name tells the next reader when the file starts applying, which is worth more than a cosmetic warning. Warnings fall 40 → 33, and the delta is exactly `IconLauncherShape` 5 → 0 and `MonochromeLauncherIcon` 2 → 0.

- **D123 — The "MOSTRE ESTA TELA" card, on six profiles of seven, with one sentence repeated three times on purpose.** Screen 17's ink card — the one the traveller holds up to a stranger — was built, tested and reachable, and **not one of the seven profiles in the real package carried `showToSomeone`**, so on the whole trip it would never have drawn. Six now do. The register is not invented here: it is copied from the packaged prototype, which is the approved wording — two clauses, polite imperative, asking for the emergency service — and the script asserts against that source rather than retyping it. **The three western-Balkan profiles carry byte-identical text, and that is correct content rather than the D102 defect.** D102 is about a field that repeats because nobody looked at it — the same sentence pasted where two different facts belonged. Here the fact itself is the same: Bosnian, Montenegrin and Croatian are one language, the itinerary says so in as many words ("Bósnio, montenegrino e croata são a mesma língua"), and *"Trebam pomoć. Molim vas, pozovite hitnu službu."* is what a stranger reads in Sarajevo, in Kotor and in Dubrovnik alike. Writing three variants to make the fields look different would be inventing dialect to decorate a data structure, and the one place it would show is the screen where being understood is the whole point. **The repetition is the finding, not a smell to be smoothed away.** The other three are the same sentence in Albanian, Greek and Dutch, each in its own alphabet and diacritics — `ë` in *nevojë*, *për*, *ndihmë*, *shërbimin*, *urgjencës*; the Greek written in Greek. **Brazil deliberately has no card.** It exists for a language barrier, and on day 1 in Guarulhos there is none; a card telling a São Paulo stranger *"Preciso de ajuda"* in Portuguese, above a Portuguese translation of itself, would be the screen explaining a problem the traveller does not have. Absent field, and screen 17 simply ends after the insurance row — confirmed on the device. **`audioAssetId` is absent rather than empty.** The schema allows it; `EmergencyState.kt:157` reads `localLanguageText` and `translation` and nothing else, and the package carries no audio but the three Sarajevo guides. An empty or invented id would be a promise of a recording that does not exist, which is D013's rule one level down. **One limit, stated because it is invisible from the data:** screen 17 resolves its profile from `day.baseCityId`, and Greece is never a base — Corfu appears only in Dia 3's `cityIds`, whose base is Ksamil. **The Greek card can therefore never appear on screen**, exactly like the Greek profile's own 112 and 166 that were already there. It is written because it is correct and because the day the itinerary changes shape it costs nothing; it is not written because anyone will see it.

- **D124 — "Comer aqui" is screen 20; the handoff's own number was already taken.** The handoff is titled *Tela 11* and its reference page says "Travel Companion · tela 11", but 11 is **Fim do passeio** in the 19 canonical screens and has been since SCREEN-INDEX-v2. Two screens with one number is the kind of collision that survives for months in commit messages and test names, so the number moved before any code was written: the screen is **20**, appended to the index rather than inserted, because renumbering 19 approved screens to make room for a twentieth would break every reference in `docs/` and every "screen 17" in a KDoc to save one integer. Nothing else in the handoff changed for this: the visual specification is used as written.

- **D125 — The menu hangs off the city, not off the day number, and that dissolves the handoff's mismatch instead of patching it.** The handoff was written against a different itinerary and says so on nearly every page: "Sarajevo (dia 9)", "Mostar (dias 10–11)", "Dia N de 21", a fallback example in Ohrid, and prices in MKD and RSD. **None of it is true of this trip** — the trip is 20 days, Sarajevo is Dia 12–13, Mostar is Dia 14–15, and North Macedonia and Serbia are not on the route. The temptation is to correct each sentence. The correct move is to notice that *not one of those facts needs to exist in the code*: a menu belongs to a **city**, the app already answers "which city is this day" through `day.baseCityId` and only then `cityIds.first()` (D090), and that one call makes every day-number in the handoff irrelevant rather than wrong. `cityOfDay` is that call, written once and shared with screens 02, 03 and 17 rather than duplicated as the route table the handoff §6 suggests reading. The day count in the header comes from `content.days.size` for the same reason — "de 21" is a literal that was already false when it was written.

- **D126 — `fallbackMenuCityId` is declared in the package, never discovered by the screen.** A day whose city has no menu borrows one, and the handoff names the lender in prose: *"Mostrando os pratos de Sarajevo"*. Hard-coding Sarajevo is obviously wrong; **taking the first city that happens to carry a menu is the wrong that is not obvious**, and it is the exact rule this repository has now broken four times and guards against in `tools/check_repo.py` (D070, D077, D080, D090, D097). So the lender is a new optional field at the top of the schema, read by name. Two validator checks came with it, because a declared pointer can be declared wrong: `fallbackMenuCityId` naming a city that does not exist, and — the one that actually matters — naming a city that exists and **carries no menu**, which puts the screen straight back into the silent-empty failure the field was added to prevent. Dish ids are also checked for uniqueness across the package, since a dish id is what a screen and a future photograph both name.

- **D127 — The country and the currency on screen 20 come from the menu being shown, never from the day being viewed.** On a day with its own menu the two answers coincide and nothing is at stake. On a borrowed one they diverge, and the whole point of the amber notice is that the page is honest about whose food it is: inheriting the country from the route would print *"Culinária local · Macedônia do Norte"* over Bosnian dishes, and inheriting the currency would quote marks as denar. `MenuUiState` therefore carries `cityName`, `countryName` and `currency` taken from the menu's own city, and the day reaches only the header strip and the notice itself. This is D070's rule — the emergency profile belongs to the country the traveller is in, not to the first profile in the array — applied one screen along, and it has a test that fails the moment the borrowed page starts inheriting.

- **D128 — With no photograph there is no photograph area, which is where this deviates from the prototype.** Seventeen dishes have a written photo brief and **zero have a binary**; the handoff says so and admits it never drew the state. The prototype draws a 158dp striped placeholder on every card, so following it literally would put seventeen striped rectangles down a scrolling page — which is not a neutral placeholder at that density, it is the screen apologising seventeen times. Instead the card starts at the dish name and the photo area does not exist: `packagedPathIfPresent` decides by **real presence of the file** rather than by a promise in the package, exactly as screen 05's hero does, and a null path draws nothing. `photoCaption` is kept in the schema and in the state even with no photo, because the brief for the picture is worth as much as the picture and should not be lost with it. The day the binaries arrive, the layout above and below is unchanged and the photo simply appears.

- **D129 — The third state: a day with no menu and no fallback says so, in the register of the withheld note.** The handoff has two states, own menu and borrowed, because in its itinerary a fallback always exists. This package can be in a third: no menu for the day's city and no `fallbackMenuCityId` declared — which is precisely where it is today, since no menu content is written yet. The screen must not invent a fourth behaviour there, and it must not pick a city to borrow from (D126), so it keeps its header, keeps its working day chevrons, and says one sober sentence: *"O cardápio desta cidade ainda não foi escrito."* That is the register of `WITHHELD_NOTE` (D061, D064) — it states what is absent and does not apologise, promise a date, or pretend the screen is broken. **The screen 02 shortcut is the other half of the same rule**: it appears only when the day's own city has a menu, so the fallback is something the traveller can walk into with the chevrons but never a door Today offers them. And screen 03 does not carry the food row at all — it renders a *browsed* day while screen 20 always opens on the day being lived, and a row naming one city while opening another is D089's confusion wearing a different hat.

- **D130 — A dish carries an ordering phrase only where there is something to order, and the phrase and its translation are checked as one thing.** `$defs/dish` required `phrase` and `phraseTranslation`, on the premise that every dish is asked for at a table. The premise is false, and the real content is what proved it: of the 75 dishes in the food guide, **two** have no ordering sentence — the half-board breakfast and lunch at the Bastasi rafting camp, where the meal is included in the nightly rate, there is no à la carte menu, and the table arrives served at a communal bench. The guide is right not to have written a sentence for them; nobody says anything to anybody. Until now they simply **could not be represented**, which made the schema, not the content, the thing that was wrong. This is worth stating as modelling rather than as an exception: a dish has an ordering phrase *when there is an act of ordering*, and half board is not a rare edge — it is how a rafting camp, a pension and a fixed-menu guesthouse all work. Both fields leave `required` and keep their shape; the model defaults them to null; and **the screen draws no "Para pedir" box at all when there is no phrase**, so the card ends at the history. That is the pattern the screen already uses for the missing photograph and that `AssetResolver.packagedPathIfPresent` uses everywhere: where the data is absent the element is absent, rather than empty or filled with something invented (D128). **What the schema cannot express is that half the pair is worse than none of it**, so a validator guard says it instead: a `phrase` with no `phraseTranslation` leaves the traveller holding out a screen of Bosnian with nothing underneath telling them what they just said, and a `phraseTranslation` with no `phrase` translates nothing. Either both or neither, checked per dish, `rc=1` in production on a package that writes one — proved on all four combinations before it was recorded here.

- **D131 — The food guide is the single source, the prototype's dish text is out, and three transformations sit between the two.** `trip-package/source/itinerary/guia-gastronomico-balcas-2026.md` — 10 cities, 75 dishes, now versioned — is the only source for screen 20's content. The handoff in `docs/design/prototype/handoff-11-comer-aqui/` keeps every word of its **visual** specification and loses every word of its **content**: its dish list was written for a different itinerary, and where the two disagree the guide wins outright rather than being merged. Nothing was retyped: the menus are parsed out of the guide's bytes, and a verification pass asserts that every packaged string is a substring of the source, because 75 dishes transcribed by hand is 75 chances to improve a sentence nobody asked to have improved. Three edits are deliberate, and each exists because the guide's shape and the screen's shape differ. **(1) Pronunciation gains slashes and loses quotes.** The guide writes `"áut-smaiter"`; the approved plate draws `/tchévapi/` in monospace, and the screen prints the field as it stands. A dish carrying two pronunciations keeps both inside one pair, separated by the same middot the rest of the app uses: `/sír-ni-tsa · ze-lia-ní-tsa/`. **(2) The ordering phrase loses its parenthesis.** `- Como pedir:` carries the sentence in quotes and then a pronunciation of that sentence; only what is inside the quotes becomes `dish.phrase`. The parenthesis has no field, and inventing one to keep it would be adding a schema property to avoid discarding a convenience the guide wrote for a reader, not for a screen. **(3) The currency becomes a phrase.** The screen writes "Preços em {currency} · esta página fica offline", and "Euro (€)" does not fit that sentence. One string per **country**, identical within it: `euros (€)`, `lekë (ALL)`, `marcos convertíveis (KM)`. Country and not city, because the currency is a fact about the country and three Montenegrin cities disagreeing about how to spell it would be the kind of drift D102 keeps finding.

- **D132 — Budva gets a menu it cannot reach, and the meal order is the content's job because nothing checks it.** Two things about this content are worth writing down before they look like mistakes. **Budva is never a `baseCityId`** — the trip passes through it on Dia 5 on the way to Kotor, and screen 20 resolves its city from the day's base (D090) — so its nine dishes can be reached from no day at all. They are written anyway: the menu is correct content about a real city on the route, it costs nothing, and the day the itinerary gives Budva a night it is already there. This is the same shape as the Greek "MOSTRE ESTA TELA" card of D123: correct data with no route to it, recorded rather than left to be discovered. **And the order of the meals is guaranteed by the guide, not by the app.** Screen 20 renders `menu.meals` in the order the content declares (D129), and no validator checks that order, so "Café da manhã, Almoço, Jantar, Doce e café" holds because the guide is written that way in all ten cities and the parser preserves it. That is a real gap with a cheap fix — a check that the four canonical names appear in canonical order — and it is named here rather than assumed away.

- **D133 — Screen 05 gains a block of editorial sections, by approval rather than by correction, and a section's body is split into paragraphs on the way to it.** `attraction.historySections` has been in the schema since the beginning and parsed into `TripModels.kt` as `List<EditorialSection>` since the models existed. **Nothing read it.** `grep -rn "historySections" app/src/main/java --include=*.kt` returned the two model declarations and nothing else: the field was parsed and discarded, every time, on every screen. That was not an oversight — the approved sheet for screen 05 specifies exactly one stretch of prose, *"Texto editorial. Parágrafo de 18px/27px"*, which is `summary`, and there has never been a surface for long-form narration. What changed is that there is now something to put there: the audio guide carries **127 titled sections across 47 attractions**, two or three per place. Without this block that text would enter the package and reach no screen at all, which is the defect class this repository has been closing since `generalEmergency.note` (D114) and the eleven null `refId`s (D118). **This is an addition to an approved screen and the approval is explicit** — `CLAUDE.md` forbids changing the 19 canonical screens without it, and this has it. The sheet is not being corrected; it is being extended, and the record says so rather than letting a later reader find an undocumented block and assume it drifted in. **Where it sits is the one design decision.** Between the summary and the operational strip: the sheet is emphatic that editorial and operational do not mix — "o horário nunca fica dentro do parágrafo editorial" — and the rule holds in both directions, so narration does not drift down beside the departure time either. Final order: hero, chips, summary, **sections**, operational strip, actions, what to observe, Plan B, fixed bar. **The paragraph split is the part that is easy to get wrong.** `editorialSection.body` is a single string and written narration puts several paragraphs inside it, separated by a blank line; handing the whole string to one `Text` is three hundred words in one slab. It is split on the blank line — runs of any length count as one break, and `\r\n` breaks as `\n` does — and each piece is drawn as its own `Text`. The split happens in the state builder rather than in the composable so that "three paragraphs stay three" is a fact a plain unit test can hold, which is how the rest of this app is arranged. **Absent stays absent**: an attraction with no sections draws no heading and reserves no space, exactly as the missing photograph and screen 20's absent "Para pedir" box do, and a regression test asserts that a section-less attraction produces a state identical to the one it produced before this commit. All 27 attractions in the real package are section-less today, so nothing on screen moves until the content lands. **`interestingFacts` is left exactly as it was**: also in the schema, also parsed, also read by nobody, and also with no place on the approved sheet. It is named here so the next person finds a decision instead of a second discovery — the difference is that no written content is waiting for it, and a surface built for content that does not exist is the opposite of what this one is. `city.historySections` is untouched for the same reason: same shape, but no city content in this round.

- **D134 — Three of the fifty audio-guide entries become nothing, and three coordinates carry a caveat the package cannot hold.** The audio guide covers 50 places; **three of them — the Sebilj fountain, Ferhadija and the Latin Bridge — are marked `**Não usar:**` in the source and are dropped whole**, not merged, not partly used. They already exist as `stories` with recorded audio and they are the three stops of `walk.sarajevo.bazar-ao-rio`, the only walk in the package. Turning them into attractions as well would put the same narration on two screens in two forms — one spoken on the walk, one read on screen 05 — and the walk's version is the one that was recorded. 47 entries remain: 27 that enrich existing attractions and 20 that become new ones. **The coordinates are the first content this package has ever carried for `location.geo`.** All 27 existing attractions now have one, and the guard that has been waiting since D098/D100/D101 ran against real data for the first time: **six cities checked, nothing accused**, and eight cities holding a single point that cannot be compared with anything. **Three of the coordinates are declared approximate by the source and the package cannot say so.** Entry 16 (Ledena Pećina — *"aproximadas; a cerca de 2.160 m, na rota clássica de Žabljak"*), entry 20 (Cachoeira Šipčanica — *"na margem do Tara, entre Brštanovica e Šćepan Polje"*) and entry 40 (Caverna Betina — *"na costa, sob os penhascos ao sul da cidade velha, em frente a Lokrum"*). They are packaged anyway: `location.geo` feeds a map link and a walking-directions intent, not turn-by-turn navigation to a deadline, and a point within a few hundred metres of a cave mouth on a mountain trail is worth more than no point at all. But **`geoPoint` has no field for a confidence, so the honesty the source took the trouble to write is lost at the package boundary** — recorded here because the three are exactly the ones where a traveller might otherwise trust the pin further than the source meant it. **One thing the coordinate does not fix**: screen 05's *"Como chegar"* now resolves to `google.navigation:q=43.8594,18.4326` — a point — because `directionsAction` prefers `geo` when it exists; but *"Abrir no mapa"* still resolves to `…/maps/search/?api=1&query=Bascarsija+Sarajevo`, because that button comes from the attraction's own `actions` array, which this phase deliberately did not touch. Half the screen moved to coordinates and half did not, and closing that is a content change to `actions` rather than to `location`.

- **D135 — The audio guide's sections replace the three written from the itinerary, and the blank line between paragraphs is content.** 76 sections now sit on the 27 existing attractions, two or three each, in the order the source writes them. **Three attractions already carried a section** — `attr.butrinto.sitio`, `attr.butmir.tunel` and `attr.mostar.stari-most`, one apiece, written from the detailed itinerary in an earlier pass when nothing read the field at all (D118). They are **replaced, not appended**: each is a one-sentence compression of ground the audio guide covers in three sections, so keeping both would print the same fact twice on one screen, once badly. The audio guide is the text written for this surface; the itinerary's was a note taken when there was no surface. **The blank line between paragraphs is the load-bearing detail.** `editorialSection.body` is a single string and screen 05 splits it on the blank line to draw one `Text` per paragraph (D133); a body joined with a single `\n`, or with none, renders as one slab of three hundred words and **nothing in the package or the validator would say so** — the schema sees a valid non-empty string either way. The separation is therefore preserved byte for byte from the source, and checked after packaging: **50 of the 76 sections carry more than one paragraph, and none carries a stray single newline that would collapse.** That count is the assertion; a future edit that flattens a body is caught by it and by nothing else.

- **D136 — The 20 new attractions carry no `practical` and no `subtitle`, because the source has neither, and Dubrovnik's card list is now thirteen.** The audio guide's 20 `**Atração:** nova` entries become full attractions: `id`, `cityId`, `name`, `summary`, `whatToObserve`, `location` with a coordinate, `historySections`, and one `maps` action. **They do not get `practical` and they do not get `subtitle`.** The audio guide is narration, not an operations sheet: it carries no price, no opening hours, no recommended duration, and no one-line strapline. Screen 05 handles both absences already — no chip row when there is no `practical`, no line under the hero name when there is no `subtitle` — so the honest card is the one that omits them. Inventing a subtitle from the first sentence of a section, or a price from the trip's other documents, would be writing content in a session meant to move it, and it would put an unchecked fact on the operational half of a screen whose whole design keeps editorial and operational apart. **The ids are short and distinctive rather than mechanical slugs** — `attr.dubrovnik.lokrum`, not `attr.dubrovnik.ilha-de-lokrum` — matching `attr.kotor.muralhas` and `attr.mostar.stari-most` already in the package. `mapsQuery` is derived and never authored: the entry's own name cut at its first comma, parentheses dropped but their contents kept, folded to ASCII, plus the city. **Dubrovnik goes from 4 attractions to 13, and that is the finding the brief asked for.** Screen 04 draws its attractions in a **horizontal carousel**, so thirteen cards are a sideways scroll rather than a wall of page — the list does not become unreadable, it becomes long to reach the end of, and the ninth card takes eight swipes. Nothing was cut to make it fit: five of the nine are the Dubrovnik Pass candidates for the free afternoon of Dia 17 and they belong together. If a carousel is the wrong shape for a city with thirteen, that is a screen-04 design question and not a reason to withhold content.

- **D137 — Six editorial bodies carried the document that produced them, because the section parser knew two heading levels and the source uses three; and paragraph counts, not substring checks, are what catch that.** The parser that turned the audio guide into `historySections` ended a section at the next `### ` and at the next `## `, but **not at `# `** — the guide's country headings (`# Albânia`, `# Bósnia e Herzegovina`, `# Croácia`, `# Montenegro`, `# Países Baixos`). So the **last section of every country block** ran past its own end and absorbed the `---` rule, the country title after it, and — in the last block of the file — the document footer, the whole `## Dúvidas` list and the whole `## O que mudou` changelog. **Six sections, always an attraction's last:** `attr.amsterdam.museu-van-gogh` (4 paragraphs → 2), `attr.butrinto.sitio` (4 → 2), `attr.sarajevo.vijecnica` (4 → 2), `attr.pocitelj.vila` (4 → 2), `attr.bastasi.sipcanica` (3 → **1**) and `attr.dubrovnik.forte-imperial` (**9 → 2**), the last of these ending the trip's final attraction card with an editor's changelog. Every body is truncated at its first paragraph beginning `---`; nothing else in the package is touched. **The reason this shipped is the shape of the check that passed it.** Phase 2 and phase 3 verified that every packaged paragraph was a substring of the source — and every one of these *was*: the changelog is really in the guide, just not under that `###`. A substring check can only find invented text; it is blind to text that is real and misplaced. **The check that replaces it compares, for all 127 sections, the packaged paragraph list with the source's paragraph list under the same `###` — count and text, element by element** — and it is run before the fix as well as after, so it is known to fail on the defect rather than merely to pass on the result: 121 of 127 identical, 6 mismatched, then 127 of 127. It also settles a claim D135 made too narrowly: "none carries a stray single newline that would collapse" was true of the 76 sections it counted, and Forte Imperial — added a phase later — held two, inside the swallowed markdown lists. They were **neither paragraph separators to promote nor mid-sentence breaks to flatten**: they were list-item separators in text that should never have entered the package, and the truncation removes them with the lists. Zero stray single newlines remain in any body of any of the three copies. **`attr.bastasi.sipcanica` genuinely leaves one paragraph, not two** — the source writes that section as a single paragraph — and the source, not the expected shape, is what the count is compared against.

- **D138 — `attr.sarajevo.catedral` and `attr.sarajevo.rosas` share a coordinate on purpose, and the pair the source doubted never entered the package. Open for Vinícius; no coordinate invented.** The package has **exactly one duplicated `location.geo`: `43.8592, 18.4258`, held by `attr.sarajevo.catedral` (Catedral do Sagrado Coração) and `attr.sarajevo.rosas` (Rosas de Sarajevo)**. They are different places and the two cards will drop the same pin. **It is not a transcription slip**: entries 26 and 27 of the guide carry that value deliberately, and entry 27 explains itself in a parenthetical the coordinate parser read past — *"(a mais visível, diante da catedral; há outras ao longo da Ferhadija)"*. The rose being described is the one at the cathedral's door; there is no single coordinate for "Rosas de Sarajevo", because there are many, and the guide chose to point at the one a visitor will actually be standing over. So the shared pin is defensible content, and **the open question is a product question rather than a data error**: whether two cards a metre apart should both link to the same point, or whether the rose's card should say in words that it stands in front of the cathedral. **No coordinate was invented and none was moved.** **The pair the source itself flagged is a different one and is not in the package.** Its `## Dúvidas` notes that entries **24 (Ferhadija)** and **25 (Chama Eterna)** share `43.8590, 18.4230` and that the street's coordinate points at its western end rather than anywhere central — but entry 24 is one of the three `**Não usar:**` entries dropped whole under D134, so `attr.sarajevo.chama-eterna` sits alone at that value and the collision the source worried about does not exist here. Recorded so that a later reader who finds the source's note does not go looking in the package for a duplicate that was never there.

- **D139 — The audio guide doubts its own line on the Blagaj sufi order, and the text stands unchanged.** The guide's `## Dúvidas` raises entry 35: *"a narração atribui a fundação à ordem Bektashi; parte das fontes fala em Naqshbandi como ordem original ou posterior. A tekke passou por mais de uma ordem ao longo dos séculos, e a formulação pode merecer checagem."* The sentence is packaged as written, in `attr.blagaj.tekke`, section **"Um mosteiro onde o rio nasce"**: *"Esta foi fundada no século 16, logo depois da conquista otomana da Herzegovina, para a ordem Bektashi, e mais tarde passou a outras ordens."* **It is not edited.** Rewriting it would mean choosing between two readings of a sixteenth-century foundation on a content session's own authority, and the clause *"e mais tarde passou a outras ordens"* already carries the part both readings agree on. Blagaj falls on **Dia 12**; this is a caption's worth of history on an enrichment screen, not a fact anyone navigates by. Open for Vinícius, alongside D138.

- **D140 - The Podgorica connection is a Dia 5 deadline, and moving it to the day costs the alert its target.** `ci.podgorica-conexao` (`actionByTime` **09:20**, *"Segunda perna sai de Podgorica às 09:38"*) was declared on `transport.ksamil-budva.night`, the night bus that leaves Ksamil on **16/09** at 19:30 and reaches Budva on **17/09** at 11:07. That transport is named in `transportIds` of **Dia 4 alone**, and `TripRepository.criticalItemsFor` gathers a day's items from `day.criticalItems`, `day.transportIds` and `day.accommodationIds` - so the connection was harvested on Dia 4, and `criticalAlerts` built its instant the only way it can, day-date + `actionByTime` + zone: **`2026-09-16T09:20` in `Europe/Tirane`**. The boarding is on **17/09 at 09:38**. The alarm rang **twenty-four hours early**, and the critical card stood on screen 02 of the wrong day - beside a day whose real difficulty is a 19:30 bus, telling the traveller to be on a platform in another country. **The item moves whole** into Dia 5's `criticalItems` (`2026-09-17`, which declares `Europe/Podgorica`, the connection's own zone) and leaves the transport; **no copy is left in both**, because D012 merges duplicate declarations rather than dropping them, so a second copy would have kept the Dia 4 occurrence alive and `criticalAlerts` keeps the *earliest* of the two. `ci.nightbus-ksamil` **stays on the transport**: its 19:15 really is a Dia 4 deadline. The Dia 5 timeline row `d05.podgorica` at 05:00 is untouched and keeps its `criticalItemIds` - it declares no `timeZone` of its own, so it contributes nothing to `zoneOverrides`, and it does not need to, because the day it sits on already declares the right zone. **The cost, stated rather than hidden:** `ownersOf` maps only the items declared by a day's transports and stays, so the item has no owner any more and the alert's target falls from `AlertTarget.Transport(transport.ksamil-budva.night)` to `AlertTarget.Day(day05)`. The notification now opens **the day**, not the transport sheet. Accepted, because Dia 5 is where the 05:00 row describes the wait, the 4h38 and the second operator - but it is a real behaviour change and it is recorded as one. **Proved arithmetically**, by replaying `criticalAlerts` over the package before and after: of the thirteen deadlines, twelve keep their date and their minute exactly, and only this one moves - `2026-09-16T09:20+02:00[Europe/Tirane]` becomes `2026-09-17T09:20+02:00[Europe/Podgorica]`.

- **D141 - The three Sarajevo stories declare `autoPlayInWalk`, and screen 10 goes back out of reach.** `StoryTriggering.decideStoryTrigger` plays inside the walk only when `walk.automaticStories && chosen.trigger?.autoPlayInWalk == true`. The walk declares no `automaticStoriesDefault` and that field's schema default is **`true`**, so the left side always held; **not one packaged story declared `autoPlayInWalk`**, and that field's schema default is **`false`**, so the right side never did. Every real arrival in Baščaršija therefore resolved to `Notify`. That contradicts the invariant Walk Mode is built on - *headphones + screen off* - because a notification has to be read, and reading it means taking the telephone out of a pocket, which is the one thing the mode exists to avoid. `story.sarajevo.sebilj`, `story.sarajevo.encontro-de-culturas` and `story.sarajevo.ponte-latina` now carry `"autoPlayInWalk": true` inside their `trigger`. **The consequence, named rather than discovered later:** with auto-play on, the offered case cannot happen in the real package, so **screen 10 is unreachable again** - the content gap D078 describes and D065 names. That is the correct behaviour and not a regression, but it means screen 10 is seen only through the debug-only arrival scaffold of D031, on an emulator, and never on the trip. **D078 is corrected in place**, because its "what is not reachable" sentence asserted the opposite of what the package actually did.

- **D142 - `criticalItemsFor` ignores the timeline's own declaration of which day a deadline belongs to; naming it, not fixing it.** D140's root cause is not the placement of one item. `CriticalAlerts.zoneOverrides` reads `timelineItem.criticalItemIds` - the timeline already states *this deadline belongs to this day* - while `TripRepository.criticalItemsFor`, which decides which items a day collects at all, reads only `day.criticalItems`, `day.transportIds` and `day.accommodationIds`. The declaration exists; the collector does not consult it. So a timeline row can correct the **zone** of an item it names and cannot correct its **date**, and **any** deadline belonging to a different day than the entity carrying it will repeat D140 - silently, with a plausible wall clock on the wrong date. The class is not exhausted by the one instance found. The fix is to have `criticalItemsFor` also union the items named by the day's timeline rows, which is Kotlin, a change to how alerts are collected, and new tests: **not a content session's change, and not a change to make seven days from departure without a defect that demands it**. D140 removes the only known instance. Recorded as an opportunity for an implementation session rather than left to be rediscovered from a wrong alarm on the road.

- **D143 - The fifty guides are one batch at `-b:a 64k` mono, and the number that goes in the notes is the one the files actually measure.** All 50 audios were re-generated from `audioguia-balcas-2026.md` with a new voice and converted in a single pass: `ffmpeg -i <in>.wav -c:a aac -b:a 64k -ac 1 -movflags +faststart <out>.m4a`. There was no neighbour to match: the three story guides that already existed are **replaced by this same batch**, so every second of audio in the package now comes from one encoder invocation with one setting. 64 kbps mono over a 24 kHz source is generous for a single speaking voice, and the whole set costs **34.99 MB** for **70.6 minutes**. **What `-b:a 64k` actually produced is not 64 kbps**, and the notes say so rather than repeating the request: FFmpeg's native AAC encoder treats the figure as a target and overshoots on this material, and the measured result is **68.1-68.5 kbps of AAC stream, 68.6-70.0 kbps of file**. Recorded because a package that claims 64 and holds 69 is a note that its own files contradict - the same class of defect D078 was corrected for. Incidentally it is also why the three replaced files sound like the ones they replace at the bit level: those were ~69 kbps too. **Every `durationSeconds` in this batch is read from the final `.m4a`**, from its `mvhd` atom, by `validate_trip.audio_duration_seconds` - the same reader the validator uses to check the number back. Nothing is estimated from the WAV, from the word count, or from what ffmpeg prints while converting; D095's 2-second guard would catch the difference anyway, and did (see D146). The WAVs stay in `audio/source-wav/` and are never promoted: they are authoring input, 194 MB of it.

- **D144 - Audio filenames carry the city, because the bare slug collides and would have dropped two guides in silence.** The rule handed to this session was `attractions/<slug>.m4a`, the attraction id without its prefix - `attr.dubrovnik.lokrum` becomes `lokrum.m4a`. Applied to the real 47 it produces **45 filenames**, not 47: `attr.budva.casco-antigo` and `attr.kotor.casco-antigo` both want `casco-antigo.m4a`, and `attr.kotor.muralhas` and `attr.dubrovnik.muralhas` both want `muralhas.m4a`. The second conversion of each pair overwrites the first, no tool complains, and two cities end up narrating another city's monument - a defect that surfaces on a wall in Kotor, not in a validator. So every file is named **`<cidade>-<slug>.m4a`**, all 47 under one rule rather than 43 under one and 4 under another: a uniform rule cannot collide when the 48th attraction arrives, and a split rule invites exactly this again. It is also **the convention the package already used**: the three story files have been `sarajevo-sebilj.m4a`, `sarajevo-encontro-de-culturas.m4a` and `sarajevo-ponte-latina.m4a` since 2026-09-05. **The ids are untouched by this** and stay exactly as specified - `ag.<cidade>.<slug>` and `asset.ag.<cidade>.<slug>` - because they already carry the city and never collided. Reversible in one pass if it is wanted the other way: rename the files and rewrite the 47 `asset.path` strings. **One inconsistency is knowingly left standing:** the three story audio assets keep their older ids `audio.sarajevo.<slug>`, while the 47 new ones are `asset.ag.<cidade>.<slug>`. Renaming three ids that nothing is wrong with, seven days from departure, buys tidiness and risks a dangling reference.

- **D145 - The fifty audios were mapped to the package by the document, and the twenty the document calls `nova` by exact name and city.** `audioguia-balcas-2026.md` declares `**Cidade:**` and `**Atração:**` on every entry, and the filename was never used to decide what a file belongs to. Twenty-seven entries name an attraction id outright and were taken verbatim. Three - **22, 24 and 28** - carry `**Não usar:** já existe como história com áudio`, and their audio replaces the three Sarajevo story guides rather than creating an attraction. The remaining twenty say `**Atração:** nova`, which was true when the document was written and is not true now: the editorial migration created all twenty, so each was resolved by **exact match of the entry's place name against `attraction.name` within the declared city** - `Palácio do Reitor` -> `attr.dubrovnik.palacio-do-reitor`. All twenty matched exactly, with no fuzzy matching and no near-miss to adjudicate, and the resolution **asserts a bijection**: 47 entries onto 47 attractions, each used once, or the script stops. The place name is the entry heading up to its editorial annotation (`Palácio do Reitor — candidato para a tarde do Dia 29`), which is also what the `audioGuide.title` becomes (D148). Two entries would have been mis-assigned by name alone and were not, because the document names their id: entry 40 is *Caverna Betina* and belongs to `attr.dubrovnik.caiaque`, entry 41 is *Stradun (Placa)* and belongs to `attr.dubrovnik.cidade-velha`. That is the reason the rule is *read the document first*.
