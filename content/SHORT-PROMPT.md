# Short Prompt

Use this after the AI has access to the Travel Companion repository and the trip source material.

> Gere o pacote de conteúdo do Travel Companion para esta viagem.
>
> Leia e siga `content/CONTENT-GENERATOR.md` como instrução principal. Use `trip-package/schema/trip.schema.json` como contrato de runtime.
>
> Trabalhe apenas em `trip-package/generated/`. Não altere código Android, design, schema nem `trip-package/production/`.
>
> Pesquise e faça fact-check quando necessário, nunca invente dados operacionais, registre gaps/conflitos e valide o pacote antes de encerrar.
